# Master prompt — Agent F3 (Sonnet): why the tracker sees ~1.3 GOOD/day while replay says ~5 (T-11, read-only diagnosis)

Work in a worktree: `cd /Users/bballi/Documents/Repos/snapshot_tradingview && git worktree add ../snapshot_tradingview-gapcheck -b gap-check && cd ../snapshot_tradingview-gapcheck && ln -s ../snapshot_tradingview/node_modules node_modules`. Read `CLAUDE.md`, `docs/AGENT_SESSION_RULES.md`, `docs/FREQUENCY_STUDY_2026-09-26.md` (branch `frequency-study`: `git show frequency-study:docs/FREQUENCY_STUDY_2026-09-26.md`), `scripts/replay-rules.js` (branch `frequency-study`), `scripts/tracker/collect.js`, `scripts/tracker/aggregate.js`, `lib/servedCalls.js`, `services/scalpContext.js` (config snapshot / configVersion), `config/engine.json`. The tracker repo is `/Users/bballi/Documents/Repos/edittrades-tracker` (run `git fetch origin` there; read `origin/main:data/…`, do not commit there).

Question: the replay of the live rules over 85 days scores ~5 GOOD/day; the live tracker (captures every 10 min since 2026-09-24) has counted 4 GOOD in ~3 days (~1.3/day). Which is it, and why? Read-only: no config, engine, or rule changes; no deploy; no orders.

## Steps
G1 **Same window, same rules.** Replay the live config (`L0`) over the tracker's own window (2026-09-24T00:00Z → now) using the newest history you can assemble (`test/fixtures/history/deep60-2026-09-24/` ends 2026-09-24T14:33Z; capture the missing days with `node scripts/replay.js --capture BTC,SOL,ETH --out test/fixtures/history/gap-2026-09-26/ --backfill-1m <minutes>` — Kraken public reads only). List every GOOD first-`ready` event (symbol, tf, direction, close time, entry/stop/TP1).
G2 **What the tracker captured** in the same window: from `edittrades-tracker` `origin/main:data/calls/*` (and `data/engine/`, `data/alerts.jsonl`), list every GOOD call it recorded, plus every capture timestamp. Also list the Telegram GOOD alerts in the same window (`data/alerts.jsonl` kinds GOOD / GOOD_ENDED).
G3 **Reconcile** the two lists event by event. For each replay GOOD, classify: (a) captured by the tracker, (b) missed because it was `ready` for fewer minutes than the 10-min capture cadence (state how many minutes it stayed ready), (c) present in the served payload but not counted by `collect.js`/`aggregate.js` (say which rule dropped it: dedup by candidateId, class change, levels missing, timeframe filter, …), (d) config/version difference between the fixture run and prod (compare `configVersion` and the config snapshot in the served-call records vs `config/engine.json`), (e) data difference (Kraken candle revisions, derived-3m differences, mark vs close). Count per class.
G4 **Cross-check with the Telegram cron**: it runs every minute; its GOOD/GOOD_ENDED alert log is the closest thing to a 1-minute record. Compare its GOOD count in the window to both lists.
G5 **Report** `docs/GAP_CHECK_2026-09-26.md`: the reconciliation table, counts per class, the single largest cause, and what a fix would be (e.g. tracker capture every minute, or ingest the cron's per-minute GOOD log, or a served-call-based count) — described, not implemented. Under 400 words plus the table.
G6 Commit on branch `gap-check` by file name (report, any new fixture manifest under `test/fixtures/history/gap-2026-09-26/` if small; otherwise leave fixtures untracked and say so). Do not push.

## Hard rules
No changes outside the report, fixtures, and (only if needed) `scripts/replay-rules.js` tooling. Nothing in `lib/`, `services/`, `config/`, `api/`. Never stage files you did not create. No env, no secrets, no deploy, no orders.
