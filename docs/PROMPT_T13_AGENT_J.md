# Master prompt — Agent J (Sonnet): net-floor shadow plan + trade chart on entry (T-13)

Work in a worktree: `cd /Users/bballi/Documents/Repos/snapshot_tradingview && git worktree add ../snapshot_tradingview-netfloor -b net-floor && cd ../snapshot_tradingview-netfloor && ln -s ../snapshot_tradingview/node_modules node_modules`. Read `CLAUDE.md`, `docs/AGENT_SESSION_RULES.md`, `docs/GAP_CHECK_2026-09-26.md`, `docs/GOOD_QUALITY_REPLAY.md` (V6: stop ≥ 0.5×ATR(15m)), `lib/flagTradePlan.js` (`shadowVariants`, `netRiskReward`), `services/scalpContext.js` (`FLAG_PLAN_SHADOW_VARIANTS`, payload caps), `lib/chartRender.js` (`buildChartSpec`, overlays, `renderContextChart`), `lib/telegram.js` (`formatGoodAlert`, Plan card, `formatResultCard`, `sendPhoto`), `api/telegram-cron.js` (GOOD alert send), `api/telegram-webhook.js` (Plan / Took it / live fill result), `scripts/tracker/` (shadow tiles: `vb-shadow*`), `test-chart-render.js`, `test-telegram.js`, `test-scalp-context.js`.

Why (owner, 2026-09-26): the first five scored GOOD calls were +0.47R gross but −2.37R net. Stops of 0.02–0.07 % on BTC are smaller than Jupiter's round-trip cost (0.34 % long / 0.14 % short), so even winners lose. The owner wants (A) a fee-aware "net floor" measured in shadow beside the live rules until the freeze ends 2026-10-08, and (B) to SEE entry, stop and target on a chart the moment a trade is ready or taken, with a plain-language line that says whether the trade is worth taking net of fees and how to approach it.

## Part A — net-floor shadow (no live rule change)
A1 Add shadow variant `NF` to `FLAG_PLAN_SHADOW_VARIANTS`: stop distance floored at `max(0.5 × ATR(15m), 3 × directionCost)` (directionCost = 0.34 % long / 0.14 % short of entry), TP1 kept at the measured move (so R:R falls as the stop widens), plan `ready` only when **net R:R ≥ 1.0** and gross ≥ 2.5. Publish it as `flagTradePlan.shadow.NF` (compact: `{ready, stop, tp1, grossRR, netRR, stopPct}`) and on `flagRecommendation.setup.shadowNF` = `{ready:boolean, netRR}`. Payload guard 79,000 B must hold (drop fields, never raise the guard).
A2 Tracker: score `NF` like the existing V-B shadow (`scripts/tracker/vb-shadow*` pattern): calls/day, fills, win %, gross and net expectancy, side by side with live on the page and report. Backfill from the served-call records it can read.
A3 Telegram: the GOOD card and the Plan card gain one line: `net floor: READY (netRR 1.4, stop 0.41 %)` or `net floor: NOT YET — stop 0.04 % < 0.31 % floor`. No alert-level or dedup change.

## Part B — trade chart on entry / taken
B1 `lib/chartRender.js`: a `tradeOverlay` on `buildChartSpec` request: entry (solid), stop (red, solid), TP1 (green, solid), optional TP2; shaded risk band (entry↔stop, red 15 %) and reward band (entry↔TP1, green 15 %); the NF shadow stop as a red dashed line when it differs from the live stop; a small legend `R 1:2.8 gross · 1:0.9 net`. Symbol/timeframe from the plan; last 120 candles of the plan's timeframe.
B2 Send it: (1) with every GOOD / GET IN NOW alert from the cron (one `sendPhoto`, the card as caption ≤ 1000 chars, within the existing chart budget); (2) on **Took it** and on a **live FILLED** result (the result card as caption); (3) `/chart <SYM> <tf> trade` renders the current plan's overlay on demand. Keep `/flags` albums unchanged.
B3 Caption "approach" block (3 lines, plain words, from data already in the payload): `Enter: <retest hold / breakout close> at <entry>` · `Wrong if: close beyond <stop> (<stopPct>)` · `Worth it: <YES / NO> net of fees — costs <x %> of a <stopPct> stop` (YES when net R:R ≥ 1.0). Reuse `clarity.killIf` and `clarity.otherSide` wording when present.
B4 Tests: overlay geometry (bands, lines, legend text), NF shadow math (floor by ATR vs cost, ready gate, mirrored short), compact payload fields + guard, cron sends photo with caption on GOOD, webhook sends photo on Took it and live FILLED, `/chart … trade`. All 14 suites + `test:tracker` green; `git diff --check`; `npm run check:gpt` unchanged (no GPT text change).
B5 Docs: `docs/PLAN_TELEGRAM.md` (chart on entry), `docs/EDITTRADES_MCP_CONNECTOR.md` schema rows for `shadow.NF` / `setup.shadowNF` (if that file is clean in your worktree; else list the rows in the handback), CHANGELOG. Commit on `net-floor` by file name; do not push.

## Hard rules
Live rules unchanged: `flagPlan.minRR` 2.5, room/chase/retest rules, alert levels, caps, gates. NF is shadow-only. No new libraries (pureimage is already there). Never stage files you did not change. No env, deploy, orders.

## Handback
Commits · test counts before → after · payload bytes before/after · one sample chart PNG path from a test fixture · NF numbers from the backfill (calls/day, net exp) · anything not implemented.
