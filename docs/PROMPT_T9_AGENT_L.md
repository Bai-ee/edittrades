# Master prompt — Agent L (Sonnet): goals + confidence-tiered risk with a Boost button (T-9)

Work in a worktree: `cd /Users/bballi/Documents/Repos/snapshot_tradingview && git worktree add ../snapshot_tradingview-boost -b risk-boost && cd ../snapshot_tradingview-boost && ln -s ../snapshot_tradingview/node_modules node_modules`. Read `CLAUDE.md`, `docs/AGENT_SESSION_RULES.md`, `lib/execution/riskPolicy.js`, `lib/execution/executor.js` (`preflight`, `createTicket`, `confirm`, `status`, `riskPrefs`), `lib/telegram.js` (`formatTicketCard`, `ticketKeyboard`, `formatRiskStatus`, `parseRiskArgs`, `applyRiskPrefsChange`, `normalizeRiskPrefs`), `api/telegram-webhook.js` (order / open / confirm / risk paths), `lib/flagRecommendation.js` (`qualityBand`, `clarity.gate`, `readiness`, `action`), `docs/PLAN_TELEGRAM_EXECUTION.md` ("Risk policy"), `test-risk-policy.js`, `test-execution.js`, `test-telegram.js`. If branch `net-floor` (T-13) has landed on main, also read its `shadow.NF` fields.

Owner (2026-09-26): "setting goals and being able to adjust confidence to boost leverage when the trade looks really strong." Trading wallet is moving to the main wallet (~$2,100), so sizing now matters.

## Deliverables
L1 **Confidence tier** per order, computed in `lib/execution/riskPolicy.js` from fields already on the recommendation the ticket came from (never from the mark): tier `A` = ready plan (`GET IN NOW`) AND `qualityBand` top band AND `clarity.gate.passable` AND (when present) `shadow.NF.ready`; tier `B` = ready plan without all of those; tier `C` = `Open (early)` / manual `/order` with no plan. Expose `tier` and the reasons on the order and the ticket card (`tier A · ready · band high · gate ok · net floor ok`).
L2 **Tier multipliers** on the risk-per-trade percent: A 1.5×, B 1.0×, C 0.5× (defaults; `/risk tiers 1.5,1,0.5` overrides, each bounded 0.25–2.0). The result is still capped by the 2 % per-trade ceiling, the exposure caps, the drawdown state, and every env cap. Leverage suggestion unchanged (stop-distance rule); the multiplier moves size, not leverage, except that a tier-A order may use the full stop-allowed leverage while B/C are capped at 50 % of it (owner intent: "boost leverage when the trade looks really strong"; keep the liquidation rule intact).
L3 **Boost**: a `Boost ×1.5` inline button on the ticket card (single use, same 60 s nonce): re-runs preflight with the next tier's multiplier (C→B→A, never beyond A's 1.5×), replaces the ticket (new nonce, old one cancelled), and the card shows `boosted from B to A · risk $X → $Y`. Confirm + PIN unchanged. Audit `boost` event with from/to tier and the risk numbers. Boost is refused when the drawdown state is on, when exposure would exceed the cap, or when equity is unavailable.
L4 **Goals**: `/risk goal <targetEquityUsd> by <YYYY-MM-DD>` stored in `state.prefs.risk.goal`; `/risk goal off`. `/risk` shows `goal $3,000 by 2026-11-01 · equity $2,140 · needed +$860 (40 %) · pace needed 1.2 %/day · pace actual 0.4 %/day · behind`. Behind/ahead never loosens anything; when **ahead of pace by ≥ 25 %**, the daily drawdown limit tightens by the same fraction (protect the gain), and the ticket card says `goal: ahead, dd limit 2.25 %`. No auto-sizing from the goal.
L5 **Status + docs**: `/exec` gains `tier mults` and `goal` rows; `docs/PLAN_TELEGRAM_EXECUTION.md` "Tiers, Boost, Goals" section; connector doc command rows (list in the handback if that file is dirty); CHANGELOG; the tracker `how-to` sentence (describe, orchestrator syncs).
L6 **Tests**: tier derivation (all three, with and without NF), multipliers and bounds, boost path (ticket replaced, old nonce refused, refusal cases), goal parsing/pace math/tightening, `/risk` and ticket rendering, audit lines. All 14 suites + `test:riskpolicy` green; `git diff --check`. Commit on `risk-boost` by file name; do not push.

## Hard rules
Nothing loosens a hard cap or gate: env caps, 2 % per-trade ceiling, 3 % stop guard, drift guard, kill, PIN, liquidation rule all bind after tiers and boost. No engine rule changes. Never stage files you did not change. No env, deploy, orders, secrets.

## Handback
Commits · test counts · the exact tier rule and multiplier table · what Boost can and cannot do · goal math example · anything skipped.
