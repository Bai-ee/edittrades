# Master prompt — Agent K (Sonnet): codebase tidy-up audit + "Risk & sizing" site page + how-to refresh (T-14)

Work in a worktree: `cd /Users/bballi/Documents/Repos/snapshot_tradingview && git worktree add ../snapshot_tradingview-tidy -b tidy-up && cd ../snapshot_tradingview-tidy && ln -s ../snapshot_tradingview/node_modules node_modules`. Read `CLAUDE.md`, `docs/AGENT_SESSION_RULES.md`, `docs/DOCUMENTATION_INDEX.md`, `docs/MASTER_PLAN_NEXT_STEPS.md`, `package.json`, `vercel.json`, `scripts/tracker/how-to-page.js`, `scripts/tracker/build-page.js`, `scripts/tracker/page-style.js`, `lib/execution/riskPolicy.js`, `lib/execution/gates.js`, `lib/execution/executor.js` (`preflight`, `checkIntent`), `lib/riskEngine.js` (`maxLeverageForStop`, `sizePosition`), `lib/telegram.js` (`formatTicketCard`, `formatRiskStatus`, `formatExecStatus`, `parseRiskArgs`), `docs/PLAN_TELEGRAM_EXECUTION.md`. Tracker repo: `/Users/bballi/Documents/Repos/edittrades-tracker` (read `origin/main`; do not commit there; the orchestrator runs `npm run tracker:sync` and deploys).

Owner (2026-09-26): "tidy things up before additional features; see if anything I forgot and what might be beneficial vs not. I don't understand the risk management and trade-amount strategy; make a standalone page on the website that describes how to approach that, like the how-to for the whole product, and make sure all of that is updated."

## Part A — tidy-up audit (report first, deletions in a separate commit)
A1 `docs/CODEBASE_AUDIT_2026-09-26.md`, three tables:
  - **Half-built / legacy code**: every `api/*.js` not on the current product path (current = scalp-context, mcp rewrite, journal, telegram-webhook, telegram-cron, health) with what it does, what it imports, whether `vercel.json` routes it, and a keep / delete / archive recommendation. Known: `analyze*`, `indicators`, `scan`, `agent-review`, `parse-trade-image`, `review-trade` (no route), `execute-trade` (gated legacy). Same for `services/` modules only they use (`marketData`, `strategy`, `scanner`, `drift/mango/jupiterSwap/positionManager/tradeExecution/perpsProvider/binance/dflow/coingecko/tokenMapping/strategy-refactored`, `jup-perps-wrapper.cjs` now unused by the live path) and `lib/` analysis modules only `analyze*` use.
  - **Orphan tests** (in the repo, not in `package.json` scripts): list each, what it exercises, whether it needs network or a real key, and keep-as-script / delete / fold-into-a-suite. Known list: test-ai-agent, test-analyze-full, test-htfbias-tdz-fix, test-jupiter-integration, test-market-capacity, test-open-perp-position, test-perp-trade, test-perps-connection, test-perps-wrapper, test-position-request-pda, test-position-request-seeds, test-provider-integration, test-query-perpetuals, test-query-position-request, test-reverse-engineer-pda, test-simple-transaction, test-strategy-override, and `test-risk-policy.js` (needs its `test:riskpolicy` script line, pending the volume-context commit).
  - **Plans**: every `docs/PLAN_*.md`, `docs/MASTER_PLAN_*.md`, `docs/PROMPT_*.md` with status (done / partial / plan-only / superseded), what remains, and worth-doing (yes / later / drop) with one line of reasoning. Include: T6 completion Step C/D, 3b real positions, Phase 3 docs alignment + Miss 004, `PLAN_TRADING_MODEL_QUICK_PASS`, `PLAN_GOOD_QUALITY` Step B (superseded by T6?), `MASTER_PLAN_TRADING_MODEL`, `PLAN_FLAG_DETECTION_COVERAGE` (F1 shipped?), the four TODOs in `services/tokenMapping.js` and `services/jupiterPerps.js:841,1505`, the stale local git branches (`feature/jupiter-perps-integration`, `optimization`, `restore-working-system`, `map-board`, `main`) and the seven worktrees.
  - Plus a short **"forgot / beneficial next"** list from what you saw (max 8 bullets, each one line).
A2 Safe deletions, ONE separate commit, only items that are unrouted AND unimported by any `package.json` script or live path: remove the files, drop dead `package.json` scripts, update `docs/DOCUMENTATION_INDEX.md` and `docs/ARCHITECTURE_MAP.json` if it lists them (if that file is dirty in your worktree, list the edits in the handback instead). Never delete `api/execute-trade.js`, anything in `lib/execution/`, `services/jupiterPerps.js`, `services/walletTracker.js`, `services/walletManager.js`, fixtures, or anything a `test:*` script runs. All 14 suites green after.

## Part B — "Risk & sizing" page + how-to refresh (tracker site)
B1 New page `docs/risk.html` in the tracker repo, built by a new `scripts/tracker/risk-page.js` wired into `build-page.js` the same way `how-to-page.js` is (same Nothing-style `page-style.js`, dark/light, mobile). Linked from the nav of `index.html` and `how-to.html`. Plain language, second person, the owner is the reader. Sections:
  1. **The three layers that size a trade**: (a) the venue (Jupiter: 100x max, fees 0.34 % long / 0.14 % short round trip, keeper fills, liquidation ≈ 1/leverage − 0.3 % maintenance), (b) the hard caps in Vercel env (`EXECUTION_MAX_SIZE_USD`, `_MAX_LEVERAGE`, `_MAX_LOSS_USD_PER_TRADE`, `_MAX_DAILY_LOSS_USD`, `_MAX_OPEN_POSITIONS`, `_MAX_ENTRY_DRIFT_BPS`; current values 20 / 100 / 2 / 25 / 1 / 15), (c) the wallet-aware risk policy (defaults 0.5 % of equity per trade, 25 % exposure, 15 % per symbol, 3 % daily / 8 % weekly drawdown auto-kill, 0.05 SOL gas floor; `/risk` shows and `/risk pct 1` tightens, never above 2 %).
  2. **How a trade amount is computed**, with one worked example at the bot wallet's real equity (read it from `/risk`-equivalent: ~0.12 SOL): risk $ = equity × pct; size = risk $ ÷ stop distance %; leverage suggestion = the max the stop allows (`100 / (stop % + 0.35 %)`, capped by env) — table for 0.3 / 0.65 / 1 / 1.5 / 3 % stops; margin = size ÷ leverage; what the ticket's `risk`, `exposure`, `suggested $X` lines mean.
  3. **Why net R matters more than gross**: the first five GOOD calls (+0.47R gross, −2.37R net) and the fee-vs-stop arithmetic; the net-floor shadow (`NF`, T-13) and the `net floor: READY / NOT YET` line; rule of thumb: don't take a stop tighter than ~3× the fee.
  4. **How to approach a trade, step by step**: alert → Plan card → `Open @ plan` vs `Open (early)` → ticket → PIN → phases card → `/positions` (SL ✔ TP ✔) → manage buttons (SL→BE, Close 50 %, Close, Set SL/TP) → journal → tracker. What `/kill` and `/arm` do. Focus mode while in a position.
  5. **Growing size safely**: raise env caps only after N clean trades; keep loss/trade ≤ 0.5–1 % of equity; the drawdown kill is the backstop; fund the bot wallet (`JEAzPi…TjwT2`) rather than trading the main wallet.
  6. **What the numbers on the tracker mean** for risk: expectancy gross vs net, losing streak, exposure.
B2 Refresh `how-to.html` (same page builder): add Focus mode, `Open (early)` / `Open @ plan`, `/risk`, `/exec` fields, the clarity lines (WAIT / STAND DOWN / Kill if / Other side), 100x cap with the stop-distance rule, charts on entry (T-13, describe as "when it lands" if not merged yet), the 1-minute GOOD log (T-12). Link to `risk.html`. Remove anything stale (dry-run-only wording, old caps).
B3 Tests: `test-tracker.js` builds `risk.html` and the nav links; `how-to` snapshot test updated. `git diff --check`. Commit on `tidy-up` by file name; do not push; do not touch the tracker repo (the orchestrator syncs).

## Hard rules
No engine, execution, or rule changes. Never stage files you did not change. No env, deploy, orders, secrets. Facts on the page come from code and docs, not memory: cite the file next to each number in a comment in `risk-page.js`.

## Handback
Audit tables (paste), deletion commit hash + list, page sections built, anything you could not verify from code.
