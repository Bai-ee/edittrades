# EditTrades MCP Connector and Scalp Stop-Distance Guard

Last updated: 2026-09-24 (delivery pass: readiness call `flagRecommendation.action`, room line `flagRecommendation.room`, one-line DATA; schema 1.25.0)
Branch: `upgrade-signal-engine`
Payload: `schemaVersion` 1.23.0, `configVersion` 2026.09.24-3 **live** (confirmed by prod check 2026-09-24 - Step A + B2 deployed after the Vercel daily quota reset: F1 through T6 completion plan Step B2, V-B engine-side shadow, `docs/OWNER_DECISIONS_2026-09-24.md`); 1.24.0 / 2026.09.24-4 local, **not yet deployed** (T6 completion plan Step C: C1 dir-cost `config.risk.costBpsByDirection`, C2 SETUP tier `flagRecommendation.setup`, C3 tracker config-boundary marker + net-R everywhere, C4 `FLAG_TF_RANK`/`FLAG_TF_ORDER` forward-compat, C5 GPT instructions/connector doc) - stops for the gate, per plan. 1.25.0 local (delivery pass: `flagRecommendation.action` + `flagRecommendation.room`, configVersion unchanged), **not yet deployed**.
Commits: `06ddb1d` (connector, wallet tracking, guard, call-path tests), `843c16f` (`stopSource` on SCALP_1H); engine phases 1–8 per `docs/MASTER_PLAN_ENGINE_REFINEMENT.md`
Shared spec page: https://claude.ai/code/artifact/0fa3fa7e-96f1-4931-b09f-565282bb9215
Thread handoff (local, outside repo): `~/Documents/ChatGPT/EditTrades/THREAD_HANDOFF.md`

## What this is

One read-only MCP tool, `get_scalp_context`, served stateless at `POST /api/mcp`, beside the existing Bearer-protected `GET /api/scalp-context` Custom GPT Action. Both read the same `buildScalpContext()` in `services/scalpContext.js` and narrow it with the same `filterPayload()`. Neither proxies the other.

ChatGPT uses it to decide TAKE / WAIT / PASS independently from the engine recommendation plus raw data. Trade execution is out of scope and unreachable from this path.

## Files

| File | Role |
| --- | --- |
| `services/editTradesMcp.js` | McpServer factory, single tool registration, `TOOL_INPUT_SCHEMA`, `runGetScalpContext`, summary text |
| `lib/mcpHttp.js` | Stateless Streamable HTTP dispatch, per-request server/transport, CORS, 405/500 handling |
| `api/scalp-context.js` | Shared Vercel function; branches to MCP on `__mcp=1`; parses `symbols`/`include`/`compact` query params |
| `vercel.json` | `/api/mcp` rewrites to `api/scalp-context.js?__mcp=1`; unmatched `/api/*` falls to `?__unknown=1` and is logged (Hobby 12-function cap) |
| `services/scalpContext.js` | Builds the payload: timeframes, strategies, `decisionTrace`, risk blocks, `candidateSetups`, `geometryContext`, `flagTradePlan`, `flagRecommendation`, `pathOutlook`, `breakoutEntry`, config snapshot, `account`; `filterPayload` |
| `services/strategy.js` | Strategy engine and the scalp stop-distance policy (see below) |
| `config/engine.json`, `config/engine.js` | Versioned, frozen engine constants; `configVersion` (Phase 1) |
| `lib/riskEngine.js` | Leverage cap from stop distance, loss at stop (Phase 3) |
| `lib/patternDetector.js` | 1m/3m/5m flag detector, long and short (Phase 4); proto/reclaim/expired states, cheap geometry fields (F1) |
| `lib/patternLifecycle.js` | Geometry snap, coil resolution, visual gate (Phase 9); `identifyCandidate` stable identity (F1) |
| `lib/candidateQualifier.js` | Trade qualification (`qual`: quality/decision/reasons) layered on candidates, never a detection input (F1) |
| `lib/freshness.js` | Pure closed-candle freshness gate (one interval + grace period); feeds `flagTradePlan`'s fail-closed check, never `dataStatus` (signal-reliability minimum plan) |
| `lib/flagTradePlan.js` | The one engine-owned flag trade plan per symbol: entry/stop/tp1/tp2, net R:R, ready/conditional/rejected + reasonCode (signal-reliability minimum plan) |
| `lib/modelEvidence.js` | Opt-in 21/200 evidence: EMA21/EMA200 map, higher-timeframe flags, channels/levels, Stoch RSI divergence |
| `lib/flagRecommendation.js` | Deterministic GOOD/WATCH/BAD/DATA_UNAVAILABLE recommendation record built from `flagTradePlan` plus model evidence |
| `lib/pathOutlook.js` | T4 P1: measured-history scenario weights (`symbols.<SYM>.pathOutlook`) for the symbol's live flag candidate, from the backoff table in `config/engine.json`'s `pathOutlook` key. Pure; imports only `config/engine.js` and `scripts/tracker/flag-paths.js`. Info only, never a gate |
| `scripts/build-path-table.js` | Dev-only (`npm run paths:table`): builds `config/engine.json`'s `pathOutlook` backoff table from a T4 P0 labelled-rows JSONL (`scripts/replay-paths.js --out`); never runs on the request path |
| `lib/breakoutEntry.js` | T4 P4 SHADOW MODE: `symbols.<SYM>.breakoutEntry`, a breakout-close entry for the pathOutlook candidate when it is the first close beyond breakoutLevel and chase is elevated/high, gated on the same `flagPlan.minRR`/`scalp.maxStopDistancePct`. Pure; imports only `config/engine.js` and `scripts/tracker/breakout-entry.js`. Tracking only, never a gate - see `docs/BREAKOUT_ENTRY_SHADOW.md` |
| `scripts/tracker/breakout-entry.js` | Pure `shadowEntryFromBreakout`/`walkShadow`/vendored `netRiskReward`, copied flat into the tracker repo by `sync.js`; imports only `./walk-outcome.js` |
| `scripts/replay-breakout-entry.js` | Dev-only (`npm run replay:breakout`): replays the T4 P0 labelled-rows JSONL against the shadow entry and a retest-hold comparison entry, prints/writes expectancy tables; never runs on the request path |
| `scripts/paper-ledger.js` | Local, append-only forward-paper ledger (`record`/`score`); never account/wallet data, never a server write API (signal-reliability minimum plan) |
| `lib/geometry.js` | Pivots, horizontal zones, ATR, room, EMA slope, diagonals, channel, confluence (Phases 7–8) |
| `services/walletTracker.js` | Read-only tracked wallet: margin, holdings, gas, performance. No keypair. |
| `lib/pythMark.js` | Read-only Pyth mark (P1): one Hermes request per build, Bearer `PYTH_API_KEY`, never throws; `symbols.<SYM>.mark` beside `price` |
| `openapi/scalp-context.yaml` | REST Action schema for the Custom GPT (source of truth for field-level detail) |
| `CHATGPT_ACTION_SETUP.md` | REST Action setup |
| `api/journal.js`, `lib/journalSchema.js` | T2 trade journal: `POST`/`GET /api/journal`, bearer `JOURNAL_API_KEY` (or the Action's `SCALP_CONTEXT_API_KEY`), Vercel Blob append (`journal/YYYY-MM-DD.jsonl` + `journal/manifest.json`). REST only, never an MCP tool; imports only `crypto`, `@vercel/blob`, the pure schema and `lib/blobJsonl.js` |
| `lib/servedCalls.js`, `lib/blobJsonl.js`, `scripts/tracker/records.js` | T3 served calls (`docs/PLAN_SERVED_CALLS.md`): the REST JSON 200 path records the unfiltered payload's calls to Blob `served/YYYY-MM-DD.jsonl` + `served/manifest.json` in the tracker's row shape (`source: served`). `blobJsonl.js` = the journal's ETag read-modify-write helpers, shared; `records.js` = the tracker's pure row builder and sensitive-key strip, shared. Not reachable from MCP |
| `api/telegram-webhook.js`, `api/telegram-cron.js`, `lib/telegram.js` | T-1 Telegram bot (`docs/PLAN_TELEGRAM.md`): owner-only read commands and `/log`, plus a once-a-minute alert cron (state in Blob `telegram/state.json`). Read-only toward the engine, never MCP, never execution; `/log` writes through `api/journal.js`'s own `appendRecord` with `source: "telegram"` |

Tests:

| Command | File | Passing (2026-09-23, Phase 2) |
| --- | --- | --- |
| `npm run test:sltp` | `test-strategy-sltp.js` (section 6b = call-path guard tests) | 50 |
| `npm run test:scalp` | `test-scalp-context.js` | 121 |
| `npm run test:mcp` | `test-edittrades-mcp.js` | 52 |
| `npm run test:wallet` | `test-wallet-tracker.js` | 28 |
| `npm run test:config` | `test-engine-config.js` (T6 completion plan A7: `setConfigOverride` throws on `configVersion`/`thresholds`) | 19 |
| `npm run test:risk` | `test-risk-engine.js` | 24 |
| `npm run test:pattern` | `test-pattern-detector.js` (fixtures `test/fixtures/flagFixtures.js`) | 33 |
| `npm run test:geometry` | `test-geometry.js` (snapshot `test/fixtures/geometryPhase7Snapshot.json`, builders `test/fixtures/geometryFixtures.js`) | 36 |
| `npm run test:chart` | `test-chart-render.js` (sample `test/fixtures/chart-sample-btc-4h.png`) | 18 |
| `npm run test:replay` | `test-replay.js` (replay harness, metrics, miss log `test/fixtures/misses/`, outcome scoring, MISS_003, exact-plan scoring) | 36 |
| `npm run test:bias` | `test-bias-matrix.js` (bias matrix, alignment, decision inputs, mirrors, td:/a200: trace grammar) | 15 |
| `npm run test:topdown` | `test-top-down.js` (weekly aggregation/lean, top-down vote, above/below-200; trading-model quick pass Q3) | 15 |
| `npm run test:freshness` | `test-freshness.js` (`lib/freshness.js`; signal-reliability minimum plan work package 1) | 10 |
| `npm run test:flagplan` | `test-flag-trade-plan.js` (`lib/flagTradePlan.js`; hand-built cases + full-pipeline 4h-flat/parity, work package 2; T6 phase 1 net gate/`costR`/`stop_inside_costs`; T6 completion plan A3 stop-wicked-retest fixture) | 51 |
| `npm run test:flagrec` | `test-flag-recommendation.js` (`lib/flagRecommendation.js`; GOOD/WATCH/BAD/DATA_UNAVAILABLE, mirrored long/short, reason fidelity; T6 completion plan A2 net_rr_below_min/stop_inside_costs) | 28 |
| `npm run test:flagrec:fixtures` | `test-flag-recommendation-fixtures.js` (Phase 2 acceptance set, pinned clock: class, primaryReason, context codes, changeConditions text, byte-stable; mirrored long/short) | 18 |
| `npm run test:ledger` | `test-paper-ledger.js` (`scripts/paper-ledger.js`; append-only forward-paper ledger, work package 3) | 12 |
| `npm run test:evidence` | `test-model-evidence.js` (`lib/modelEvidence.js`; EMA map, channels, divergence) | 8 |
| `npm run test:mark` | `test-pyth-mark.js` (`lib/pythMark.js`; mock Hermes: expo/conf, one request, no key → no request, failures → unavailable, drift sign, stale; `dataStatus` untouched) | 12 |
| `npm run test:tracker` | `test-tracker.js` (`scripts/tracker/`, T1 call tracker: collector strips account/wallet/balance/address keys before disk, dedupe, candle store, vendored `walkOutcome` parity, scorer synthetic day, idempotency, aggregates, page from an empty store, charts; T2 journal pull/score/your-trades line/wallet ticks/Engine vs you; T3 served pull/dedupe/day window, served scoring parity, activity counts, Via column; T6 phase 1 restart note; Telegram sent-alert/transition ingest, alert latency/outcome joins, Alerts zone; dev only, never on the request path) | 114 |
| `npm run test:journal` | `test-journal.js` (T2: `lib/journalSchema.js` validation, `api/journal.js` auth 401, method 405, body cap 413/400, rate limit 429, idempotency on `id`, ETag retry, GET newest-first/limit; journal imports nothing that signs or executes; MCP neither imports the journal nor registers a journal tool; Blob put/get injected, no network; T6 completion plan A4 first-write-race retry) | 19 |
| `npm run test:paths` | `test-flag-paths.js` (T4 P0: `scripts/tracker/flag-paths.js` path labeller retest_go/runner/false_break/fail_first/chop long+short, no lookahead, retest only after the breakout candle closes, retest-hold parity with `lib/flagTradePlan.js`, feature buckets, base rates; T6 completion plan A3 stop-wicked-retest parity + `labelPath` regression; dev only) | 28 |
| `npm run test:replay-paths` | `test-replay-paths.js` (T4 P0: `scripts/replay-paths.js` first-tightening selection, row wiring, report buckets; dev only) | 19 |
| `npm run test:served` | `test-served.js` (T3: served rows stripped + credential guard, dedupe, manifest, day rollover, kill switch/no token/unavailable, store error swallowed, 1500 ms timeout; handler records once on the JSON 200 with the unfiltered payload, never on 401/405/500/503/chart, response identical with and without the hook; MCP does not import the recorder; Blob injected, no network; T6 completion plan A5: the response is sent before a slow/throwing recorder finishes) | 22 |
| `npm run test:outlook` | `test-path-outlook.js` (T4 P1: `lib/pathOutlook.js` candidate-selection precedence, backoff key resolution, tightening/broken table choice, lean/likely/chase rules, null cases, the SOL 2026-09-24 shape against the real measured table, purity) | 27 |
| `npm run test:breakout` | `test-breakout-entry.js` (T4 P4 SHADOW MODE: `scripts/tracker/breakout-entry.js` purity + `netRiskReward` parity with `lib/flagTradePlan.js`, `shadowEntryFromBreakout` rule conditions long+short, `walkShadow` outcomes incl. prefilled/open/expired, `lib/breakoutEntry.js` purity + the RULE - pathOutlook.at/chase, `ageCandles===0` "first close" gate, immutability; dev/tracker only, never on the request path) | 33 |
| `npm run test:rules` | `test-replay-rules.js` (T6 phase 0: `scripts/replay-rules.js` variant table, `config/engine.js`'s `setConfigOverride` override hook end to end, scoring/stats pure functions, V5/V6 structure-plan construction; dev only, never on the request path) | 25 |
| `npm run test:archmap` | `test-architecture-map.js` (`docs/ARCHITECTURE_MAP.json`: shape, every lib/services/api/config/scripts/tracker file listed exactly once, no stale paths, current schemaVersion has a CHANGELOG.md entry, listed test scripts exist, flows/outputs resolve; writes `docs/ARCHITECTURE_MAP.verify.json` for the tracker's system map page; dev only) | 10 |
| `npm run test:telegram` | `test-telegram.js` (T-1: formatters per class incl. the SETUP line and DATA block, alert transitions/dedup by candidate id, data/mark 5-min persistence and 30-min repeat, heartbeat write budget, command and `/log` parsing into the journal schema, webhook 405/403/503/allowlist silence, `/log` idempotent through the journal append path, cron 401/503 and send-once under overlapping runs, tracker Alerts fact, alert levels per class, WATCH dedup/cooldown/triggering pass-through, Chicago quiet hours incl. CDT/CST and weekend, `/alerts` parsing and prefs persistence, reply keyboard and label mapping, inline button callbacks incl. Took it / Skipped journaling and callback allowlist, no execution/signing import reachable, two-line alerts with one bold verdict and void-drift signature dedup; Plan / Thesis cards, track lifecycle, TP1/stop on mark long + short, close/partial/positions journaling, nudge once, timeframe focus, `/market`, visual layout; sent-alert log line shape and strip check, transition diff, both logs written once, log failure never blocks a send; Bot API and Blob mocked, no network) | 90 |

The first four are the deploy gate; the rest are the per-module suites added by the engine phases. Run every `test:*` script in `package.json` before a deploy, plus `check:gpt` and `git diff --check` (`test:tracker`/`test:rules` cover out-of-band tooling, not the request path).

Counts re-run 2026-09-24 (T2): `test:pattern` 33, `test:flagplan` 43, `test:flagrec` 18, `test:flagrec:fixtures` 16, all others as listed. T3 (2026-09-24): `test:served` 18 (new), `test:tracker` 31 → 42, `test:journal` 17; deploy gate unchanged (sltp 50, scalp 117, mcp 52, wallet 28). T6 phase 0 (2026-09-24): new `test:rules` 25. T6 phase 1 (2026-09-24, net gate ships on 2.0): `test:flagplan` 43 → 50; `test:flagrec`, `test:flagrec:fixtures`, `test:tracker` unchanged in count (assertions updated for the renamed `net_rr_ok`/`fees_heavy` tokens and the restart note); deploy gate unchanged (sltp 50, scalp 117, mcp 52, wallet 28). T6 completion plan Step A (2026-09-24, `docs/PLAN_T6_COMPLETION_V2.md`, review pass A1-A7): `test:config` 14 → 19, `test:flagplan` 50 → 51, `test:flagrec` 18 → 20, `test:paths` 22 → 28, `test:served` 18 → 22, `test:journal` 17 → 19, `test:tracker` corrected to its true current count 85 (was stale at 42, several untracked phases behind); `test:pattern`/`test:scalp`/`test:geometry` schemaVersion assertions updated to 1.22.0, counts unchanged (33/117/36); deploy gate unchanged (sltp 50, mcp 52, wallet 28). 848 tests across 27 suites, all green. Owner decision 4b (2026-09-24, `docs/OWNER_DECISIONS_2026-09-24.md`): `test:flagplan` 51 → 53 (two mirrored cases for the `room_at_entry`/TP1-cap timeframe split); no schema/config change. T6 completion plan Step B2 (2026-09-24, D-cost/D-variant, V-B engine-side shadow): `test:flagplan` 53 → 59, `test:scalp` 117 → 120, `test:tracker` 85 → 93; `test:config`/`test:geometry`/`test:pattern` schemaVersion assertions updated to 1.23.0, counts unchanged (19/36/33); deploy gate unchanged (sltp 50, mcp 52, wallet 28). T6 completion plan Step C (2026-09-24, C1-C5): `test:flagplan` 59 → 67, `test:tracker` 93 → 110, `test:flagrec` 20 → 23, `test:scalp` 120 → 121; `test:config`/`test:geometry`/`test:pattern` schemaVersion assertions updated to 1.24.0, counts unchanged (19/36/33); deploy gate unchanged (sltp 50, mcp 52, wallet 28). 26 suites, all green. T-1 Telegram (2026-09-24): new `test:telegram` 32; `test:journal` unchanged at 19 (records gain a server-stamped `source`, null on the GPT path); deploy gate unchanged. Telegram alert levels + quiet hours (2026-09-24): `test:telegram` 32 → 41; buttons: 41 → 49; flag chart albums: 49 → 55; BREAKOUT alert: 55 → 58; chase SETUP: `test:flagplan` 67 → 69, `test:flagrec:fixtures` 16 → 17; all other suites unchanged. Telegram state versioning + cron health (2026-09-24): `test:telegram` 58 → 64; all other suites unchanged. Telegram sent-alert + transition logs (2026-09-25): `test:telegram` 85 → 90, `test:tracker` 111 → 114; all other suites unchanged. Delivery pass (2026-09-24, schema 1.25.0): `test:flagrec` 28 (nextCloseEta, four action calls mirrored, WAIT on forming/triggering, room capped/measured mirrored), `test:flagrec:fixtures` 18 (action + room across the acceptance fixtures), `test:telegram` 67 (call prefix, Room line, one-line DATA, readiness+room on GOOD/SETUP/WATCH/TRIGGERING/BREAKOUT), `test:scalp` 121 (compact key list gains `action`/`room`); schemaVersion assertions in `test:config`/`test:geometry`/`test:pattern`/`test:scalp` updated to 1.25.0.

Replay (Phase 10, dev only, never on the request path): `npm run replay -- --capture BTC,SOL,ETH --out test/fixtures/history/<date>/ [--backfill-1m 360]` saves a live pull; `npm run replay -- --history <dir> --symbols BTC --out btc.jsonl` runs `buildScalpContext()` once per closed candle with no lookahead; `npm run replay:metrics -- btc.jsonl` prints candidate counts, visual-gate rate by code, lifetime and label precision/recall; `npm run replay:outcomes -- btc.jsonl <historyDir>` (trading-model quick pass Q4; signal-reliability minimum plan work package 3 added exact-`flagTradePlan` scoring alongside the existing strategy/`FLAG_MEASURED` rows, plus a rejection-reason breakdown) walks the same JSONL forward on 1m candles and scores every valid strategy signal, confirmed flag candidate, and selected flag trade plan: fill rate, win rate, average win R, expectancy, max losing streak, median time to TP1. Details: master plan, Phase 10; `docs/PLAN_TRADING_MODEL_QUICK_PASS.md` Q4.

Breakout-entry replay (T4 P4, dev only, never on the request path): `npm run replay:breakout -- --rows <paths-rows.jsonl> --history <dir> [--symbols BTC,SOL,ETH] [--window-hours 24] [--json <file>]` replays a `scripts/replay-paths.js --out` JSONL against the shadow breakout-close entry (chase-gated and ungated) and a retest-hold comparison entry on the same rows, reporting n, fill, win rate, gross/net expectancy R, and max losing streak overall, by timeframe, and by chase bucket. Results and the promotion recommendation: `docs/BREAKOUT_ENTRY_SHADOW.md`.

Forward-paper ledger (signal-reliability minimum plan work package 3, dev only, local file, never on the request path): `npm run ledger:record -- <context.json> [--out paper-ledger/calls.jsonl]` appends one row per symbol from a saved `buildScalpContext` payload (every `flagTradePlan`, including a genuine no-trade `null`) - append-only, refuses a duplicate `planId`/no-plan id, never carries `account`. `npm run ledger:score -- <ledger.jsonl> <historyDir> [--out outcomes.jsonl]` walks only the `ready`/`conditional` rows forward against saved 1m history (same `walkOutcome` as `replay:outcomes`) and writes a separate outcomes file - the ledger file itself is never rewritten. `paper-ledger/` is gitignored.

## Endpoint

| Item | MCP | REST |
| --- | --- | --- |
| URL | `POST https://snapshottradingview.vercel.app/api/mcp` | `GET https://snapshottradingview.vercel.app/api/scalp-context` |
| Transport | MCP Streamable HTTP, stateless, protocol 2025-03-26 | JSON |
| Auth | None (data approved for public read-only exposure) | Bearer `SCALP_CONTEXT_API_KEY` |
| Headers | `Content-Type: application/json`, `Accept: application/json, text/event-stream` | `Authorization: Bearer` |
| Options | tool args `symbols`, `include`, `compact`, `chart` | query `?symbols=BTC,SOL&include=strategies,geometry&compact=1`; `?chart=BTC:1m` returns `image/png` |
| Response | JSON-RPC in SSE frame; payload in `structuredContent` + `requestId` | Raw payload |
| `dataStatus: unavailable` | Tool error (`isError: true`) | 200 with status in body |
| Wrong method | 405 JSON-RPC `-32000` | 405 |
| OPTIONS | 200, CORS `*` | n/a |
| Handler failure | 500 JSON-RPC `-32603`, `data.requestId`, no stack text | 500 |

Trade journal (T2, REST only, not part of MCP):

| Path | Auth | Methods | Behavior |
| --- | --- | --- | --- |
| `/api/journal` | Bearer `JOURNAL_API_KEY` or `SCALP_CONTEXT_API_KEY` | `POST`, `GET` (others 405) | `POST` validates one record (`text` required, `kind` open/close/adjust/skip/note, numbers optional, optional `engineRef`), stamps `id`/`receivedAt`/`schemaVersion`, appends to Blob `journal/YYYY-MM-DD.jsonl` (ETag-guarded read-modify-write) and keeps `journal/manifest.json` (`baseUrl`, `days[]`); 201, or 200 `duplicate:true` for an `id` already stored today/yesterday; 400 invalid, 401, 413 over 4 KB, 429 over 10/min per key (in-memory, best effort per warm instance), 503 store unavailable. `GET ?limit=` (default 10, max 50) returns records newest first. Actions `postJournal`/`getJournal` in `openapi/scalp-context.yaml`, same bearer scheme as the context Action (`bearerAuth`); the journal also accepts `SCALP_CONTEXT_API_KEY` because a ChatGPT Action carries one bearer for all operations |

Telegram bot (T-1, `docs/PLAN_TELEGRAM.md`, not part of MCP, never execution):

| Path | Auth | Methods | Behavior |
| --- | --- | --- | --- |
| `/api/telegram-webhook` | Header `X-Telegram-Bot-Api-Secret-Token` = `TELEGRAM_WEBHOOK_SECRET` (else 403); sender id in `TELEGRAM_ALLOWED_USER_IDS` (else 200, no reply) | `POST` (others 405) | One Telegram update. Commands `/signals`, `/why SYM`, `/flags [SYM]` (text summary, then one `sendMediaGroup` album per symbol of every live flag: proto/forming/triggering/confirmed/expired, one chart per symbol+timeframe, max 9 images, `SYM · no live flags` when none), `/chart SYM TF`, `/wallet`, `/journal [n]`, `/status` (also alert level, quiet hours, and `Cron failures: N in a row · last failure: <reason> <time>` from `telegram/health.json`), `/log text`, `/testalert`, `/help`, `/alerts` (show level + quiet hours), `/alerts good\|setup\|watch`, `/alerts quiet HH-HH` (America/Chicago, every day), `/alerts quiet off`, `/alerts quiet` (show), `/alerts tf 5m\|3m,5m\|all` (WATCH/TRIGGERING/BREAKOUT timeframes, default 3m,5m), `/positions` (journal opens with no close: live mark, R now, R to stop/TP1, age; `Close @ mark`), `/tracking` (tracked flags, max 10, 6 h), `/market` (24h card from engine fields: change/range, top-down, 4h/1h lean, EMA200, Stoch, drift, rule-based LEAN, alerts today by kind, the 1h/4h level that would change it), `/menu`, `/charts`; `/alerts` changes are saved in `telegram/state.json` `prefs` with the ETag. Buttons: every plain reply carries a persistent reply keyboard (`Signals`, `Flags`, `Market` / `Why BTC`, `Why ETH`, `Why SOL` / `Charts`, `Wallet`, `Positions` / `Journal`, `Status`, `Alerts`, `Tracking`; labels map to the commands, case-insensitive). Inline `callback_query` buttons (same allowlist, `answerCallbackQuery` first): `chart:SYM:TF`, `flags:all` (Charts picker `All flags` → the `/flags` albums), `why:SYM`, `alerts:good\|setup\|watch`, `alerts:quiet:on` (01-05 default) / `alerts:quiet:off`, `log:took:SYM:<ref>` / `log:skip:SYM:<ref>` (ref = 8-hex FNV-1a of the candidateId; journals kind `open` / `skip` with the alert plan's entry/stop/tp1/direction and `engineRef`, id `tg_open_<ref>` / `tg_skip_<ref>` so a double tap logs once; Took it also tracks the trade), `plan:<ref>` (Plan card: levels, R gross/net, sizing from the candidate `risk` block, mark vs Kraken, expected length, call; `NOT A TRADE UNDER YOUR RULES — <reason>` otherwise), `thesis:<ref>` (supports/against/unknown/changes as phrases, To become GO IN), `track:<ref>` / `untrack:<ref>` (`state.tracked`), `closed:<ref>` / `partial:<ref>` / `stillin:<ref>` (after a TP1/stop hit on a taken trade: journal `close` / `adjust` or re-arm), `pclose:<ref>` (`/positions` Close @ mark), `alerts:tf:3m5m\|5m\|all`. Every alert and `/signals` block carries `Plan · Thesis · Chart` / `Track · Took it · Skipped`; a ref resolves live first, then the stored snapshot, else `[expired — send /signals]`. Messages use the visual layout (`<dot> <glyph> <b>SYM tf ▲ LONG</b> · KIND`, rule-separated LEVELS / VERDICT / PLAN / CONTEXT, under 1,000 chars; `docs/PLAN_TELEGRAM.md`). Register with `setWebhook` `allowed_updates: ["message","callback_query"]`. Replies via Bot API `sendMessage`/`sendPhoto`/`sendMediaGroup` (HTML, chunked under 4,000 chars, 5 s timeout). Always 200 once accepted so Telegram does not redeliver. 503 with a reason when the secret or `TELEGRAM_BOT_TOKEN` is unset. No `/buy`, `/sell`, `/open`, `/close` |
| `/api/telegram-cron` | `Authorization: Bearer CRON_SECRET` (Vercel Cron sends it), else 401 | `GET` (others 405) | Every minute (`vercel.json` `crons`). One build; compares with Blob `telegram/state.json`; sends NEW GOOD (plan levels, gross/net R, change condition, mark drift, tracker link, plan-timeframe chart), NEW SETUP (trigger sentence), GOOD ended (BAD = rejected, else void), data unavailable/stale or mark not ok for over 5 min (repeat at most every 30 min). State is claimed with the blob ETag before sending, so overlapping runs send once. Heartbeat-only state writes at most every 10 min. Alert level from state `prefs.level` (`good` = GOOD + GOOD ended; `setup` default, adds SETUP; `watch` adds one-line `WATCH`/`TRIGGERING` for new forming/triggering flag candidates, deduped by candidateId over the last 200, 15-min per-symbol cooldown, forming → triggering passes once). Health alerts always send. WATCH/TRIGGERING/BREAKOUT only on `prefs.alertTimeframes` (default 3m, 5m; null = all), plus 1m flags of a tracked symbol + direction (line 0 `1m ENTRY · for your tracked …`). Tracked candidates (`state.tracked`, Track / Took it) alert on every transition at any level: state change, SETUP, GET IN NOW with the Plan card, void/gone/expired, and TP1/stop on the mark (Kraken close fallback) once ready or taken, with Closed here / Partial / Still in and one close reminder after 10 min (dropped when the journal already holds the close). Quiet hours from `prefs.quiet` (default 01-05 America/Chicago, every day): alerts send with `disable_notification`, never dropped. After the sends, each sent alert appends one line to Blob `telegram/alerts/YYYY-MM-DD.jsonl` and each candidate state / plan-status change one line to `telegram/transitions/YYYY-MM-DD.jsonl` (day manifests; `lib/telegramLog.js`; best effort, 2 s cap, never blocks a send; off with `TRACK_TELEGRAM_LOG=false`); the tracker ingests both. 503 with a reason when `CRON_SECRET`, the token, the allowlist or the Blob store is missing |

Served calls (T3, REST side effect, not part of MCP): before sending a JSON 200, `GET /api/scalp-context` records the **unfiltered** payload's per-symbol calls (rows with a `flagRecommendation`) to Blob `served/YYYY-MM-DD.jsonl` (dedupe key `symbol|closedThrough|class|planStatus`) and `served/manifest.json` (`served-manifest-1`). Awaited with a 1500 ms cap; failures log `[Served] skipped=<reason>` and are swallowed; status, headers and body never change. Not on 401/405/500/503 or `?chart`. Off with `TRACK_SERVED_CALLS=false` or without `BLOB_READ_WRITE_TOKEN`. The tracker pulls these rows and scores them like cron rows (`source: served`, page "Via: chat").

Fresh `McpServer` + transport per request, torn down on `res.close`. `sessionIdGenerator: undefined` = stateless mode, required on serverless.

## Tool

| Field | Value |
| --- | --- |
| Name | `get_scalp_context` |
| Input | all optional: `symbols: string[]`, `include: string[]`, `compact: boolean`, `chart: string`. `{}` returns the full payload unchanged. |
| Annotations | `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true` |
| Build timeout | 25 s → tool error |

Success: `content[0].text` = one-line summary (`generatedAt closedThrough dataStatus warnings symbols requestId`); `structuredContent` = filtered payload + `requestId`.

Errors (`isError: true`, sanitized text, no structured content): build threw/timed out; empty payload; `dataStatus === 'unavailable'` ("Do not trade on this run"). `partial` + warnings pass through as success.

### Payload controls (Phase 5)

- `symbols`: any of `BTC`, `SOL`, `ETH`, case-insensitive.
- `include` tokens: `timeframes`, `strategies`, `candidates` (→ `candidateSetups`), `geometry` (→ `geometryContext`), `trace` (→ `decisionTrace`), `account`, `config`, `bias` (→ `biasMatrix`, `alignment`, `decisionInputs`, `topDown`), `model` (→ bulky 21/200 model evidence; also unlocks `flagTradePlan.shadow`, schema 1.23.0). `price`, `mark`, `source`, `structure`, `bestSignal`, `flagTradePlan` (minus `shadow`), compact `flagRecommendation`, and `pathOutlook` are always kept.
- `bias` and `model` are opt-in: the objects are only built into the response when `include` lists them (`includeBias` / `includeModel`). Without `model`, the compact `flagRecommendation` remains available. Prefer `compact` with `bias` or `model`.
- 2026-09-23 (F1, flag detection coverage): schema 1.12.0 added `qual`, `candidateId`, `firstDetectedAt`, `failedAt`, `flagSlope`, `breakoutDistancePct`, `invalidationDistancePct` to every flag candidate, plus the new `proto`/`expired` states and more candidates surviving longer (failedTtlCandles/expiredTtlCandles) - live default before any trim ranged ~79.3-83.3 KB with busier markets (multiple 1m/3m/5m proto/forming/failed candidates at once), over the 79 KB cap. `impulseStart`/`impulseEnd` were dropped from the payload as redundant (see `lib/patternLifecycle.js` `identifyCandidate` - `impulseStart` is `candidateId`'s own last segment, `impulseEnd` is always `firstDetectedAt` minus one candle) and `CANDLE_LIMITS` on 1m/3m/5m went 24 → 20; default settled at ~75.3 KB (77068 B measured), build ~840 ms.
- `compact: true`: every `timeframes.<tf>.candles` becomes `[]`; indicator summaries stay; `mark` narrows to `{price, driftBps, status}` (schema 1.16.0).
- Unknown values are ignored with one `payload controls: ...` line in `warnings`, never rejected. If every requested value is unknown the filter is treated as absent. These warnings are added after `dataStatus` is computed and do not change it.
- Measured 2026-09-23 (F1), live BTC/SOL/ETH: full ~75.3 KB (77068 B), compact ~38.9 KB (39831 B). The builder logs a soft warning above 80 KB.
- Measured 2026-09-23 (review fix pass, schema 1.15.0), live BTC/SOL/ETH: default 76,889 B (was 84,095 B measured before the pass; 82,463 → 77,011 B on one identical live snapshot), compact 41,899 B, `include=model` alone 33,005 B, full build with model 104,531 B; build ~725 ms live. Saved fixture `test/fixtures/history/2026-09-23`: default 76,586 B, compact 41,440 B. `test:scalp` asserts default ≤ 79,000 B and compact ≤ 45,000 B on that fixture.
- Measured 2026-09-23 (P1 Pyth mark, schema 1.16.0): saved fixture with injected ok marks default 77,063 B (+477 B vs 76,586), compact 41,658 B (+218 B). Live BTC/SOL/ETH with marks ok: default 75,814 B, compact 40,635 B (pre-P1 live on the same afternoon: 75,921 / 41,001 B, different market snapshot). Build ~533–736 ms live (pre-P1 531–789 ms): the Hermes read runs alongside the candle fetches.

### Confirmation chart (Phase 8b)

- `chart: "BTC:1m"` (REST `?chart=BTC:1m`): one PNG, 900×500, of that symbol/timeframe, rendered by `lib/chartRender.js` (pure JS, `pureimage`, bundled `assets/fonts/IBMPlexMono-Regular.ttf`, OFL). Never in the default response.
- MCP: `content` = `[text summary + " chart=BTC:1m", {type:"image", mimeType:"image/png", data}]`, `structuredContent` as usual. REST: `200 image/png` instead of JSON. REST auth unchanged; the param is read after auth.
- One chart per call. Two charts, an unknown symbol/timeframe, or a malformed value: MCP `isError`, REST 400 JSON. No candles for that timeframe in the build: MCP `isError`, REST 503. Rejected before the build runs.
- Draws the published candle window, EMA21/EMA200, horizontal zones (bands), diagonals, channel, candidate flag levels (dashed). 1m/3m/5m: candles + EMAs + candidate only.

## Payload schema 1.17.0

Field-level definitions live in `openapi/scalp-context.yaml`. This is the map.

Top level: `schemaVersion`, `configVersion`, `config`, `generatedAt`, `closedThrough`, `sessionTimezone`, `dataStatus` (complete | partial | unavailable), `account`, `symbols{BTC,SOL,ETH}`, `warnings[]`, `requestId` (MCP only).

`config` (Phase 5): snapshot of tunables from `config/engine.json` — `scalp.maxStopDistancePct`, `riskReward.bySetupType/byStrategy`, `risk.*`, `flag.includeFailed`.

`account`: `status` (available | partial | disabled | unavailable), `reason`, `address` (masked), `fetchedAt`, `margin{usd, byAsset}` (USDC/USDT = risk capital and P&L base), `holdings[]`, `holdingsUsd` (exposure, not P&L), `unpriced[]`, `gas{sol, minSol, sufficient}`, `performance{baselineUsd, netPnlUsd, returnPct, source}`. Unavailable is never zero. Never affects `dataStatus`.

`symbols.<SYM>`, in key order: `price`, `mark` (schema 1.16.0), `source{provider, pair, fetchedAt}`, `structure{sessionHigh, sessionLow, prevDayHigh, prevDayLow, swingHighs[5], swingLows[5], support[3], resistance[3], aboveEma21, aboveEma200}`, `timeframes{1m,3m,5m,15m,1h,4h,1d}`, `strategies{SWING, TREND_4H, TREND_RIDER, SCALP_1H, MICRO_SCALP}`, `bestSignal`, `candidateSetups[]`, `geometryContext`, `decisionTrace`, `flagTradePlan`, `flagRecommendation`, `pathOutlook`, `breakoutEntry`, then with `include=bias` only: `biasMatrix`, `alignment[]`, `decisionInputs`, `topDown`; with `include=model` only: `model`.

`mark` (schema 1.16.0, P1, `lib/pythMark.js`): `{price, conf, publishTime, source: "pyth", ageSec, driftBps, status}`. Live Pyth price (Hermes, one request for all symbols, Bearer `PYTH_API_KEY`) beside `price`, which stays the closed 1m candle close. Jupiter perps mark, stop and liquidate on Pyth, so stops/Thesis Eliminated/liquidation are checked against `mark.price`. `driftBps` = (mark.price − price) / price × 10000, 1 decimal (null when `price` is null). `status`: `ok` | `stale` (`ageSec` > `config/engine.json` `mark.pyth.maxAgeSec`, 30) | `unavailable` (no key, HTTP error, timeout, feed missing; every other field null). Unavailable never changes `dataStatus` and adds no warning (same rule as `account`). Builds with injected candles (tests, replay) get `unavailable` and make no request: a live mark beside historical candles is wrong. Compact: `{price, driftBps, status}`.

`timeframes.<tf>`: `candles[]` (closed only; 20 for 1m/3m/5m (F1, was 24), 20 for 15m/1h (review fix pass, was 24), 20 for 4h, 10 for 1d; compute runs on up to 499 regardless - `CANDLE_LIMITS` is payload-only), `ema21`, `ema200`, `priceVs21Pct`, `priceVs200Pct`, `trend`, `stochRsi{k, d, state, cross, slopeK, slopeD}`, `closedThrough`, `candleCount`.

`strategies.<NAME>` canonical: `valid`, `direction` (long | short | NO_TRADE), `confidence` 0–100, `reason`, `entryZone{min,max}`, `stopLoss`, `invalidationLevel`, `stopSource` (5m | 15m | 4h | percentage | null), `targets[]`, `riskReward{tp1RR, tp2RR}`, `entryType`, and on valid signals `risk` (Phase 3). Invalid → NO_TRADE, all levels null, targets empty.

`risk` (Phase 3, valid strategies and triggering/confirmed candidates): `maxLeverage`, `suggestedLeverage`, `lossAtStopUsd`, `lossAtStopPct`, `lossAtStopPctOfWallet`, `collateralUsd`, `reason` (null when the numbers are populated).

`candidateSetups[]` (Phase 4, F1 flag detection coverage): flag candidates on 1m/3m/5m, long and short, independent of `strategies` and `bestSignal`. `timeframe`, `type: flag`, `direction`, `state` (schema 1.12.0: `proto` | `forming` | `triggering` | `confirmed` | `expired` | `failed`), `impulseStrength`, `compressionScore`, `flagHigh`, `flagLow`, `breakoutLevel`, `invalidation`, `measuredTarget`, `measuredRR` (schema 1.11.0, trading-model quick pass Q1: pole-length projection from `breakoutLevel`; a major level in front of it takes priority as the real TP1), `ema200Side` (schema 1.11.0, Q2: `above`/`below`/`null`, never filters), `ema21Hold` (schema 1.12.0 adds `reclaim`: the flag's first candle was off-side of EMA21 but reclaimed it within `flag.reclaimCandles`), `confidence`, `chaseRisk` (always `true` on an `expired` candidate), `flagSlope`, `breakoutDistancePct`, `invalidationDistancePct` (schema 1.12.0, cheap geometry: least-squares slope of the flag's own closes and signed percent distance from the last close), `candidateId`, `firstDetectedAt` (schema 1.12.0, stable identity derived from this request's own candle window - no stored state; `impulseStart`/`impulseEnd` are not separately published, see `lib/patternLifecycle.js` `identifyCandidate`), `failedAt` (schema 1.12.0, failed only), `qual` (schema 1.12.0: `{quality, decision, reasons}`, every candidate flag or coil - see below), `risk`. A `failed` candidate is dropped from the default payload unless `config.flag.includeFailed` **or** it is still inside `flag.failedTtlCandles` of its own timeframe (it then carries `failReason`/`failedAt` regardless); older failures follow `includeFailed` as before. Schema 1.15.0: a published `failed` candidate carries only `timeframe`, `type`, `direction`, `state`, `failReason`, `failedAt`, `candidateId`, `breakoutLevel`, `invalidation`, `confidence`, `qual` (`slimFailedCandidates`; `decisionTrace` tokens unchanged; the replay harness builds with `slimFailed: false`). A `confirmed` flag more than `flag.maxBreakoutAge` candles past its break reads `expired` for `flag.expiredTtlCandles` more, then is not found at all.

`candidateSetups[].qual` (F1 item 8, `lib/candidateQualifier.js`, every candidate): `quality` (low | med | high, banding the candidate's own `confidence`), `decision` (watch | wait | dont | actionable - never a probability of profit; proto/forming → watch, triggering → wait, failed/expired → dont, confirmed with no blocking reason → actionable else wait), `reasons[]` (`conflict:<tf>-<dir>`, `stoch:ob-cross`/`stoch:os-cross`, `room:blocked-<tf>` (schema 1.17.0: only the candidate's mapped geometry timeframe, `geometryTimeframeFor`, 1m/3m/5m → 15m), `ema200:counter`, `ct:4h`, `chase`, `rr:<x>` - built only from data already elsewhere in the payload, never a new detection signal).

`config.flag.timeframes` stays `1m`/`3m`/`5m` (T6 completion plan D-variant, owner-approved 2026-09-24 - V1c does not widen detection; V-C, which does, was replayed and not chosen, `docs/GOOD_QUALITY_REPLAY.md`). 15m/1h flags are never published while this holds. `lib/flagTradePlan.js`'s `FLAG_TF_RANK` and `lib/flagRecommendation.js`'s `FLAG_TF_ORDER` (T6 completion plan C4) already carry ranked entries for `15m`/`1h` - inert today (neither ever appears in the candidate pool), ready with no code change the moment `flag.timeframes` is ever widened: trade-plan selection would then prefer the higher timeframe on a tie, WATCH candidate selection would still prefer the smaller (nearer-forming) one.

`flagTradePlan` (schema 1.13.0, `lib/flagTradePlan.js`, signal-reliability minimum plan): the one trade call built from the symbol's confirmed directional flag candidates (`type: flag`, `state: confirmed` only - coils and proto/forming/triggering/failed/expired stay observation-only). `null` when none exists this build; otherwise `candidateId`, `planId`, `timeframe`, `direction`, `status` (ready | conditional | rejected), `reasonCode` (null only when `ready`; `awaiting_breakout`/`awaiting_retest` when `conditional` - schema 1.15.0 renamed `awaiting_retest_hold` to `awaiting_retest`; stable rejection codes when `rejected`), `entryType` (`retest`), `entryCondition` (breakout-then-retest-hold text, entry rounded to 2 decimals), `entry`, `stop`, `tp1`, `tp2`, `grossRR` (schema 1.17.0), `netRR`, `stopDistancePct`, `shadow` (schema 1.23.0, T6 completion plan D-variant, `include=model` only - see below). Schema 1.17.0 (owner decision 1a): the gate is gross price R, `grossRR = |tp1 − entry| / |entry − stop|` ≥ `flagPlan.minRR` (3.0), else rejected `rr_below_min` (was `net_rr_below_3`); `netRR` after `config.risk` fees/slippage is information only and never rejects. Schema 1.15.0: `ready` requires an earlier closed candle that closed through the breakout level, then the latest closed candle's low (high for a short) within `flagPlan.entryToleranceAtr` ATR of the level and a close on the hold side; the breakout candle alone is `conditional`/`awaiting_retest`. Selection is deterministic: ready before conditional before rejected, then highest detector `confidence`, higher candidate timeframe, stable `candidateId`. Schema 1.24.0 (T6 completion plan C1, D-cost decision): `netRR`/`costR` are direction-dependent - `config.risk.costBpsByDirection[direction]` (already round-trip; a long pays 34 bps, a short 14 bps, positions funded from USDC/USDT so only a long pays the extra swap in/out) in place of the flat `feeBps`/`slippageBps` fallback (20 bps, still used when direction can't be resolved). A long and a short at the identical gross R:R can now net differently - never assume symmetric net numbers across a mirrored long/short pair.

`flagTradePlan.shadow` (schema 1.23.0, T6 completion plan D-variant, owner-approved 2026-09-24, `docs/OWNER_DECISIONS_2026-09-24.md`) - **SHADOW MODE, research only, never a trade instruction; do not act on it.** `undefined | {vB: {same shape as flagTradePlan itself, minus confidence}}`. `vB` re-runs the SAME candidate pool with `flagPlan.minRR` overridden to 2.5 (`flagPlan.minNetRR` and everything else unchanged) - real ATR, real retest-hold, no approximation - and is present only when that outcome (status/reasonCode/candidateId) differs from the live plan above; identical outcomes publish nothing. **Not in the default payload even when computed internally - visible only with `include=model`.** Owner chose to run V-B shadow-only rather than ship it; revisit 2026-10-07, n >= 20 scored plans each side (V1c live vs this) before any sign-off to ship. Tracked by `scripts/tracker/vb-shadow.js`, its own tile on the call tracker page.

`flagRecommendation` (schema 1.15.0, `lib/flagRecommendation.js`): compact 21/200 decision record `{class, setupId, candidateId, candidate, asOf, primaryReason{code,text}, readiness, setup, action, room, qualityBand, policyVersion, supports[codes], opposes[codes], unknowns[codes], changeConditions[{code,text}], trace}` - no `refs`, no `factorStates` (the full record is `model.recommendation`). Classes are `GOOD`, `WATCH`, `BAD`, `DATA_UNAVAILABLE`; a symbol with `price: null` is `DATA_UNAVAILABLE`, and with no plan a missing/stale 1m/3m/5m series is `DATA_UNAVAILABLE` with `unknowns` like `stale_data:1m`. The R:R floor is `flagPlan.minRR` only (renamed from `minNetRR` in 1.17.0). Schema 1.17.0: `rr_below_min` is BAD; a ready/conditional plan carries support `rr_ok` and, when `netRR` < `minRR`, the non-blocking oppose `net_rr_low` (fees eat the edge) - net R:R never makes it BAD. The recommendation consumes `flagTradePlan` and model evidence; it never recalculates levels/R:R and never treats legacy `bestSignal` as the 21/200 recommendation. Schema 1.18.0 (Phase 2, recommendation completeness): every record except `market_data_unavailable` cites context codes as support/oppose for the plan's or named candidate's direction (unknown when undirected) - `td:<sentiment>:<n>/4`, `a200:<above>/<of>`, `ema200:<tf>:<side>`, `ema200:1w:missing`, `4h:with`/`ct:4h`/`4h:flat`, `level:<geomTf>:<price>`/`level:none` (first level beyond the breakout on the candidate's own geometry timeframe), `tp1_capped:<price>`, `chan:<geomTf>:<edge>:<risk>`, the candidate's `conflict:`/`stoch:`/`rr:` qual codes, `divergence_*`, `data_fresh`/`data_partial`; none changes the class. `candidate{candidateId, timeframe, direction, state, breakout, invalidation, measuredRR}` names the nearest forming/triggering/proto flag on a no-plan WATCH (null otherwise). On BAD the disqualifying reason is `opposes[0]`. `changeConditions` text is concrete: the candidate's close/retest trigger, the rejection's remedy, or `a 1m/3m/5m flag must form (none detected)`. Samples: `docs/FLAG_RECOMMENDATION_REVIEW_SHEET.md`.

`flagRecommendation.setup` (schema 1.24.0, T6 completion plan C2, SETUP tier) - **informational only, never a GO IN, never gates `class`/`primaryReason`/`supports`/`opposes` on its own.** `null | {candidateId, timeframe, direction, entry, stop, tp1, grossRR, netRR, entryCondition}` - the best still-`conditional` attempt in the symbol's own candidate pool (computed once in `lib/flagTradePlan.js`'s `buildFlagTradePlan`, published on `flagTradePlan.setup` there, then stripped before publish and copied through to this field only - never duplicated in the payload). Every `conditional` status already implies the gross gate passed (`grossRR >= flagPlan.minRR`) and, when the net gate is on, the net gate too (`netRR >= flagPlan.minNetRR`) - `buildPlanAttempt` only reaches the retest-hold check after both, so `conditional` alone already means "3R-clean, just not confirmed yet." Can name a DIFFERENT candidate than `flagTradePlan` itself (e.g. `flagTradePlan` is `ready` on a 1m candidate while a separate 3m candidate is still conditional) - both are published simultaneously, never merged. Chase SETUP (2026-09-24, owner priority): when no conditional attempt exists, a confirmed flag rejected only for `chase` whose re-attempt without the chase flag clears every other gate (room, stop cap, gross/net R) is published as the SETUP, same shape, `entryCondition` "wait for a <tf> retest of <entry> that holds above\|below it"; `flagTradePlan` stays `rejected`/`chase` and `class` stays BAD. In the default payload (not gated by `include`), unlike `flagTradePlan.shadow`. GPT: `SETUP LINE` in `docs/GPT_INSTRUCTIONS.md`, appended after that asset's own GOOD/NO TRADE line in `signals`/`trades`. Tracker: `scripts/tracker/score.js`'s `kind:'setup'`/`counterfactual_setup` mode scores it what-if (walked forward only from whenever the entry level is actually touched, never prefilled - a SETUP is never entered); `aggregate.js` tiles `goodPerHour7d`/`setupsPerDay7d`; `charts.js`'s `setupEquityRows` feeds the equity chart's filter table (a "SETUPs, what-if" row, not its own drawn line).

`flagRecommendation.action` / `flagRecommendation.room` (schema 1.25.0, delivery pass, `lib/flagRecommendation.js`, default payload and `model.recommendation`) - derived only from fields the record already computes; never a gate, never changes `class`. `action = {call, etaMin, at, note}`: `GET IN NOW` on GOOD (etaMin 0, at = asOf); `BE READY` for a conditional live plan, else a SETUP (etaMin/at = next close of that timeframe after `asOf`, the symbol's 1m closedThrough - 5m at 01:12 -> 01:15, etaMin 3; note = its entryCondition); `WAIT` for a no-plan WATCH naming a forming/triggering candidate (eta to its next close, note = the change condition); `STAND DOWN` otherwise - BAD with no setup, DATA_UNAVAILABLE, WATCH with nothing forming (etaMin/at null, note = remedy/change condition). `room = null | {toLevel, levelPrice, levelSource, pts, r, stop}` for the live ready/conditional plan, else the SETUP, else a rejected plan that still has entry/stop/TP1: `levelPrice` is TP1 - the cap zone edge (`toLevel` `tp1_cap`, `levelSource` e.g. `15m resistance`, matched against every geometry timeframe's zone edges exactly as `nearestRoomAhead` scans them) or the measured target (`measured_target`, `measured move`); `pts` = |TP1 - entry|, `r` = pts / |entry - stop|. The GPT (`[CALL]`/`Room:`) and Telegram copy both verbatim.

`pathOutlook` (schema 1.19.0, T4 P1, `lib/pathOutlook.js`, always present, not gated by `include`): a measured-history read on the symbol's live flag candidate - `null | {id, tf, dir, at, lean, likely, chase, w, n, cal, key}`. Candidate choice: `flagRecommendation.candidateId`'s own candidate if still live, else `flagTradePlan.candidateId`'s, else the nearest live forming/proto/triggering/confirmed 1m/3m/5m flag (triggering > forming > proto > confirmed, then confidence, then smaller timeframe, then `candidateId`); `null` when no live flag candidate exists this build. `at`: `forming`/`proto` → `tightening`, `triggering`/`confirmed` → `broken` (the two measured tables in `config/engine.json`'s `pathOutlook` key, T4 P0's 15-day base rates, `docs/FLAG_PATHS_BASE_RATES.md`). `key`/`n`/`cal` name the exact backoff bucket used (`tf|structureSteps|roomR|compression`, dropping the last feature until `n >= pathOutlook.minN` (100), down to `tf` alone, then `'all'`) and its sample size; `cal` is `n >= minN`. `w{retest_go,runner,false_break,fail_first,chop}` are integer percents summing to ~100 (`at:"broken"` always reads `fail_first` 0 - it requires an invalidation close before any breakout close, impossible once already broken out). `lean` is (`retest_go`+`runner`) vs (`false_break`+`fail_first`), `even` within 5 points; `likely` is the highest-weight path; `chase` is `high` when the runner share is >= 1.2x the retest_go share or the candidate's own `chaseRisk` is true, `elevated` when the runner share is above the table-wide baseline, else `low`. Info only: never changes `flagTradePlan`, `flagRecommendation`, `strategies`, `bestSignal`, or any gate/threshold.

`breakoutEntry` (schema 1.20.0, T4 P4, `lib/breakoutEntry.js`, always present, not gated by `include`) - **SHADOW MODE ONLY, tracking and research, never a trade instruction; do not act on it.** `null | {id, tf, dir, at, entry, stop, tp1, grossRR, netRR, status}`, `status` always `"shadow"`. Published for the SAME candidate `pathOutlook` names (`id` matches `pathOutlook.id`), only when: `pathOutlook.at === "broken"`; the candidate's own timeframe's most recent closed candle is the FIRST close beyond `breakoutLevel` (nothing after it - `candidateSetups[].ageCandles === 0`, `lib/patternDetector.js`'s "candles since the break candle, 0 = the break is the last closed candle"); and `pathOutlook.chase` is `elevated` or `high`. `entry` is that candle's own close, `stop` is `invalidation`, `tp1` is `measuredTarget` (no geometry TP1 cap), `grossRR = |tp1 − entry| / |entry − stop|` gated on the same `flagPlan.minRR` (3.0, never lowered) and stop distance gated on the same `scalp.maxStopDistancePct` (3%, never raised) the engine uses everywhere else; `netRR` is the same fee/slippage formula `flagTradePlan.js` uses (`lib/flagTradePlan.js` now exports `netRiskReward` for this). `null` otherwise. Never an input to `flagTradePlan`, `flagRecommendation`, strategies, `bestSignal`, or any gate/threshold. Replay evaluation against the P0 15-day dataset and the promotion recommendation (do not promote yet): `docs/BREAKOUT_ENTRY_SHADOW.md`.

`model` (schema 1.15.0, `include=model`, `lib/modelEvidence.js`): bulky evidence behind `flagRecommendation`: `flags[]` including 15m/1h/4h detector reads, `ma.map` for EMA21/EMA200 value/side/distance/slope/provenance, `channels` with levels ahead and breakout risk (a fade against sentiment at the channel edge is `high`; no direction → `unknown` and no levels ahead), `divergence` from Stoch RSI pivot confirmation (Stoch history aligned to the candle series' end; most recent pivot pair wins; stale divergences not counted in confluence), and `recommendation` (the full recommendation record with refs and factorStates). Omitted by default for payload size.

`geometryContext.<tf>` (Phases 7–8; 15m, 1h, 4h by default, `null` when a timeframe lacks enough candles): `atr`, `atrPct`, `structure` (up | down | range), `higherLows`, `lowerHighs`, `horizontalSupportZones[]`, `horizontalResistanceZones[]`, `roomToNextSupport`, `roomToNextResistance`, `extensionRisk`, `ema21Slope`, `stochAccelK`, `confidence`, `diagonalSupport`, `diagonalResistance`, `channel`, `confluenceZones[]`.

`decisionTrace` (Phase 2): `configVersion`, `evaluatedAt`, `strategies[]` (`name`, `ran`, `valid`, `rejectedAt`, `reason` per strategy), `bestSignal`, `bestSignalReason`, `window` (per-timeframe `{to, closedCandles}`; Phase 11 dropped `from`, the oldest candle's open time), `candidateSetups[]` (`"1m:long:confirmed"` strings; schema 1.12.0 adds the `proto` and `expired` states, e.g. `"1m:long:proto"`, `"5m:short:expired"`; a failed one adds its `failReason`, `"5m:short:failed:stale"`, Phase 9b - unaffected by the default payload's `failedTtlCandles`/`includeFailed` gating, so a hidden failure is still named here), `geometry[]` (`"1h:up:0.42:0.18:low"` strings, room values rounded to 2 decimals as of Phase 11; a missing individual field is an empty position, `"1h:up::0.18:"`; a timeframe with no geometry at all is one token, `"1h:na"`), `needsVisualConfirmation`, `visualTarget`, `unresolvedGeometry[]` (Phase 9 visual gate; since 9b a near-miss diagonal alone never raises it and `visualTarget` prefers triggering/confirmed), `bias` (Phase 9b, `"scalp:L14,S16,N70|swing:L74,S0,N26|tf:1m=S,…,1d=L|ct:2"`, ≤ 120 bytes through schema 1.10.0; schema 1.11.0 appends up to two more tokens, `"|td:bull:3/4|a200:5/7"` — see `topDown` below; schema 1.16.0 appends `"|mark:<driftBps>"` or `"|mark:na"`, under 12 chars, e.g. `"|mark:1.2"`).

`biasMatrix.<tf>` (Phase 9b, opt-in): `bias` (long | short | neutral), `strength` 0–100, `basis[]` for 1m…1d. `alignment[]`: per non-failed candidate and valid strategy, `source`, `ref`, `direction`, `executionTf`, `contextTfs`, `withTrend`, `counterTrend`, `htfBias`, `htfBiasStrength`, `nearestHtfZoneDistancePct` (nearest HTF zone ahead: resistance for a long, support for a short), `room` (in that timeframe's ATR), `roomTooSmall`. `decisionInputs.directionalBias{scalp, swing}`: `{long, short, neutral}` summing to 100. Inputs for the GPT's own allocation; the API never emits GO IN / HOLD / DON'T.

`topDown` (schema 1.11.0, opt-in, trading-model quick pass Q3, M-1/M-2/M-6b): `sentiment` (bull | bear | mixed) from a weighted vote over `leans.{1w,1d,4h,1h}` (config `model.topDownWeights`; higher timeframes dominate), `aligned` (0-4, how many leans agree with `sentiment`), `score` (0-1 conviction), `leans{1w,1d,4h,1h}` (bull | bear | neutral each), `weekly{close, ema21, ema21Slope, ema200: null, reason}` (1W derived from the already-fetched 1D candles, Monday-aligned weeks; weekly EMA200 is always null - not enough weekly history exists - with a reason), `above200{count, of, weighted}` (of the timeframes with an EMA200, 1m-1D, how many sit above it; mirror `below200` is `of - count`). Never gates or changes a strategy, candidate, or confidence.

### Coming in Phase 8b (in progress)

Optional `chart: "BTC:1m"` tool arg / `?chart=BTC:1m` query returns exactly one server-rendered PNG (MCP `image` content block; REST `image/png`). Absent `chart`, responses stay byte-identical. See the master plan, Phase 8b. Update this section when it lands.

## Scalp stop-distance policy (`services/strategy.js`)

- `MAX_SCALP_STOP_DISTANCE_PCT` = `config/engine.json` `scalp.maxStopDistancePct` (3). `validateScalpStopDistance(entry, stop)` measures from the entry mid with `1e-9` epsilon.
- `applyScalpStopPolicy()` builds the stop via `calculateSLTP(..., 'Scalp', ...)` and enforces the gate. Both `SCALP_1H` (PRIORITY 4 in `evaluateStrategy`) and `evaluateMicroScalp` route through it. Rejection → canonical NO_TRADE with reason `Setup rejected: scalp stop distance X% exceeds 3.00% maximum`.
- Percentage fallback: for `setupType === 'Scalp'` anchors at the entry mid so it lands exactly at the policy distance. If the mid-anchored stop cannot clear the entry zone (zone wider than 3%), it falls to the edge anchor, which the gate rejects → NO_TRADE. Swing / 4H / TrendRider keep the original edge anchor, unchanged.
- MICRO_SCALP rejection sets `result.reason`; `evaluateAllStrategies` surfaces it over the generic "conditions not met".
- `normalizeToCanonical` carries `stopSource`, nulls it on invalid signals. SCALP_1H raw signal emits it.
- Tests: section 6b of `test-strategy-sltp.js` drives `evaluateStrategy` and `evaluateMicroScalp` directly, long + short × wide / tight / no-structure. Mutation check: disabling either guard fails two tests. Fallback-anchor revert fails seven.

## Security boundary

- `services/editTradesMcp.js` and `lib/mcpHttp.js` import only from `services/scalpContext.js` (`buildScalpContext`, `filterPayload`). No execution, position, or signing-wallet module reachable.
- `walletTracker.js` reads a public address over JSON-RPC; no keypair.
- Logs: `requestId`, method, status, duration, reason code, payload bytes, build duration only. No RPC URL, bearer, or upstream error text.
- Env var names not spelled in MCP sources; `test:mcp` greps for leaks.
- MCP has no network auth. A private GPT/plugin listing is UI privacy, not authentication.
- Trade execution stays behind `TRADE_EXECUTION_ENABLED` + `TRADE_EXECUTION_API_KEY`, separate from market-context auth. Never register an execution tool in MCP.
- The trade journal (`api/journal.js`) is a separate REST write path with its own key; it records text only, imports nothing that can sign or execute, and is not reachable from MCP (`test:journal` asserts both).
- The Telegram bot (`api/telegram-webhook.js`, `api/telegram-cron.js`, `lib/telegram.js`, `lib/telegramLog.js`) reads the same context build and writes only journal lines, its own alert state and its sent-alert / transition logs (no account, wallet or user fields); it imports no execution, signing or wallet-writing module, transitively (`test:telegram` walks the imports), and MCP never imports it. Secrets, message text and user ids are never logged.
- Served-call recording (`lib/servedCalls.js`) writes only the tracker's row shape: explicit field list, account/wallet/balance/address/margin/performance keys stripped, then a fail-closed re-check plus a credential check (bearer/secret/api-key keys, `Bearer ` values, URLs with key query params); any hit writes nothing. MCP never imports it (`test:served` asserts).

## Environment (Vercel production)

| Var | Purpose |
| --- | --- |
| `SCALP_CONTEXT_API_KEY` | REST Bearer |
| `TRACKED_WALLET_ADDRESS` | Public Solana address to inspect |
| `SOLANA_RPC_URL` | Read-only RPC |
| `ACCOUNT_BASELINE_USD` | Starting stablecoin margin for P&L |
| `PYTH_API_KEY` | Hermes Bearer for `mark` (P1); missing → `mark.status: unavailable` |
| `JOURNAL_API_KEY` | Journal REST Bearer (T2); missing → every journal request 401 |
| `BLOB_READ_WRITE_TOKEN` | Vercel Blob store `edittrades-journal` (public access) for the journal and served calls; missing → journal 503, served recording off |
| `TRACK_SERVED_CALLS` | Optional kill switch (T3); `false` → no served-call recording. Unset = on |
| `TRACK_TELEGRAM_LOG` | Optional kill switch; `false` → the Telegram cron writes no sent-alert or transition log lines. Unset = on |
| `TELEGRAM_BOT_TOKEN` | Telegram Bot API token (T-1); missing → both Telegram functions 503 |
| `TELEGRAM_ALLOWED_USER_IDS` | Comma list of Telegram user ids that get answers and alerts; anyone else gets silence |
| `TELEGRAM_WEBHOOK_SECRET` | Must match the `secret_token` given to `setWebhook` (register with `allowed_updates: ["message","callback_query"]` so buttons arrive); missing → webhook 503 |
| `CRON_SECRET` | Vercel Cron bearer for `/api/telegram-cron`; missing → cron 503 |
| `TELEGRAM_QUIET_HOURS` | Retired (no longer read): quiet hours are set on the phone with `/alerts quiet HH-HH` (America/Chicago), stored in `telegram/state.json` `prefs` |
| `TRADE_EXECUTION_ENABLED` | Master switch for execution (T-3, `lib/execution/`); anything but `true` → every order refused (`execution_disabled`). Not set in production |
| `EXECUTION_MODE` | `dry` (default) or `live`; any other value refuses. Dry runs the full path but never signs. Live also refuses until on-chain SL/TP exists (`LIVE_CAPABILITIES` in `lib/execution/executor.js`) |
| `EXECUTION_OWNER_IDS` | Comma list of Telegram user ids allowed to execute (separate from `TELEGRAM_ALLOWED_USER_IDS`); missing → refuse |
| `EXECUTION_PIN` | 4–8 digits, required on every confirm / close / SL-TP change / `/arm`; compared constant-time, never logged; 3 wrong within 1 h → auto-kill 1 h |
| `EXECUTION_KILL` | `true` → kill switch on (env kill; `/arm` cannot clear it). The Blob flag `execution/kill.json` (set by `/kill`) is the other kill |
| `EXECUTION_MAX_SIZE_USD` | Per-order notional cap; missing → refuse |
| `EXECUTION_MAX_LEVERAGE` | Per-order leverage cap; missing → refuse |
| `EXECUTION_MAX_LOSS_USD_PER_TRADE` | Cap on size × stop % + direction cost; missing → refuse |
| `EXECUTION_MAX_DAILY_LOSS_USD` | Cap on today's realized loss (journal closes) + this order's max loss; missing → refuse |
| `EXECUTION_MAX_OPEN_POSITIONS` | Open on-chain positions allowed; default 2 |
| `SOLANA_PRIVATE_KEY` | Signing key for the engine wallet (live execution only); loaded lazily past the gates, never logged or audited. Dry mode without it reads positions from `TRACKED_WALLET_ADDRESS` |

Missing wallet vars → `account.status: disabled`, market data unaffected. Missing `PYTH_API_KEY` → every `mark.status: unavailable`, no Hermes request, market data unaffected.

## Production verification (2026-09-22, first deploy, schema 1.1.0)

| Check | Result |
| --- | --- |
| REST no auth / bad key / POST / auth GET | 401 / 401 / 405 / 200 |
| dataStatus | complete, 0 warnings, no NaN/Infinity/undefined |
| account.status | available, margin reported |
| MCP initialize | 200, server `edittrades` |
| MCP tools/list | exactly one tool, `readOnlyHint: true` |
| MCP tools/call | 200, summary + `structuredContent` with BTC/SOL/ETH + account |
| BTC SCALP_1H | rejected, stop 6.18% > 3.00% |
| SOL / ETH SCALP_1H | valid, sub-1% stops (at first deploy) |

Not observed live: `stopSource` on a valid SCALP_1H, MICRO_SCALP guard. Covered by tests only.

Spot check 2026-09-22 evening, schema 1.8.0 live: REST no auth 401, POST 405; MCP `tools/list` one tool, `readOnlyHint: true`; `tools/call {}` → `schemaVersion` 1.8.0, `configVersion` 2026.09.22-6, `dataStatus` complete, geometry B fields present, ~78 KB response. Authed REST 200 not re-checked in this pass.

## Verify after any redeploy

```bash
U=https://snapshottradingview.vercel.app
curl -s -o /dev/null -w "%{http_code}\n" $U/api/scalp-context                       # 401
curl -s -o /dev/null -w "%{http_code}\n" -H "Authorization: Bearer $KEY" $U/api/scalp-context   # 200
curl -s -H "Authorization: Bearer $KEY" $U/api/scalp-context | jq -r .schemaVersion           # 1.8.0
curl -s -X POST $U/api/mcp -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}'                                # one tool
```

After a deploy that touches the Telegram functions: `curl -s -o /dev/null -w "%{http_code}\n" -X POST $U/api/telegram-webhook` → 403 (no secret header); `curl -s -o /dev/null -w "%{http_code}\n" $U/api/telegram-cron` → 401; `/status` on the phone answers (`Cron failures: 0 in a row`); cron logs show no `reason=state_write_` or `reason=state_reset` lines; `/testalert` delivers a TEST card with a chart; the cron sends nothing while nothing changes.

After a deploy that touches the REST path, also confirm served recording: one authed GET, then `curl -s <blob base>/served/manifest.json` lists today and `served/<today>.jsonl` has one row per symbol (a repeat GET at the same close adds none).

## Open items

- Phase 8b (confirmation chart) in progress; then 9, 9b, 10, 11, 8c, 3b per the master plan.
- Re-run the Custom GPT `SIGNAL` workflow once through the REST Action, with the updated `openapi/scalp-context.yaml` re-imported.
- `vercel.json` still routes `/api/trade-status/*` to `api/trade-status.js`, which does not exist (falls to a 404 on Vercel).
- `public/index.html` dashboard edits are separate user work.

## Work log

| Date | What |
| --- | --- |
| 2026-09-21 | Connector, wallet tracker, MCP dispatcher built. Blocker found: 3% guard rejected its own percentage fallback (edge-anchored 3% measured from mid ≈ 3.1–3.3%). MICRO_SCALP had no guard. |
| 2026-09-22 | Fallback re-anchored at mid for Scalp only. Shared `applyScalpStopPolicy` wired into SCALP_1H and MICRO_SCALP. Call-path tests added and mutation-verified. JSDoc placement fixed. Dead SCALP_1H branch in 4H block removed. `stopSource` published on SCALP_1H. Two prod deploys, verified. Wallet env vars added to Vercel by user. |
| 2026-09-22 | Engine phases 0–8: config + `configVersion`, `decisionTrace`, risk engine, flag detector + `candidateSetups`, compute-depth assertion, payload controls (tool args), geometry A and B. Schema 1.1.0 → 1.8.0, live in production. |
| 2026-09-23 | F1 (flag detection coverage, `docs/PLAN_FLAG_DETECTION_COVERAGE.md`, single implementer pass): proto state, `maxImpulseCandles` 8→20, EMA21 reclaim, failed-visible-for-`failedTtlCandles`, `expired` state, stable `candidateId`/`firstDetectedAt`, cheap geometry fields, `qual` qualification layer (`lib/candidateQualifier.js`, new module). `CANDLE_LIMITS` 1m/3m/5m 24→20 to hold the payload under budget. Schema 1.11.0 → 1.12.0, configVersion → 2026.09.23-1. Not deployed. |
| 2026-09-23 | Signal-reliability minimum plan (single implementer pass): a confirmed flag candidate, `qual.actionable`, was never a trade - no exact entry, no fees-adjusted R:R, no staleness gate. Added `lib/freshness.js` (pure closed-candle freshness gate, one interval + grace) and `lib/flagTradePlan.js` (the one engine-owned flag trade plan per symbol: ready/conditional/rejected + reasonCode, entry/stop/tp1/tp2/entryCondition, net R:R after `config.risk` fees/slippage, deterministic selection across candidates), wired into `services/scalpContext.js` as `symbols.<SYM>.flagTradePlan` - additive only, `strategies`/`bestSignal` byte-identical, 4h-flat lean never gates it. Fixed `scripts/replay-outcomes.js`'s `walkOutcome`: unfavorable zone-edge fill (was the zone midpoint) and no same-candle fill+target win credit (the false-intrabar-win bug); added gross ready-level diagnostics alongside the existing replay output. Added `scripts/paper-ledger.js` (local, append-only `record`/`score` CLI; never account data; refuses duplicate plan ids). GPT instructions: added `flagTradePlan=trade authority` (CANDIDATES) and `trades = signals.` (COMMANDS), funded by tightening existing wording - see `docs/GPT_INSTRUCTIONS.md`'s own change log. Schema 1.12.0 → 1.13.0, configVersion → 2026.09.23-2. New suites `test:freshness`/`test:flagplan`/`test:ledger`; `test:replay` 31 → 35. Not deployed. |
| 2026-09-23 | 21/200 decision clarity follow-on: fixed retest readiness to require a closed-candle hold, made ready outrank conditional, relabeled replay/ledger scoring as gross ready-level diagnostics, added `lib/modelEvidence.js`, `lib/flagRecommendation.js`, `symbols.<SYM>.flagRecommendation`, opt-in `include=model`, docs `TRADING_MODEL_DECISION_CONTRACT.md` and `FLAG_RECOMMENDATION_REVIEW_SHEET.md`, and GPT schema 1.14.x language. Schema 1.13.0 → 1.14.0, configVersion → 2026.09.23-3. New suite `test:flagrec`. Not deployed. |
| 2026-09-23 | Review fix pass: flag plan `ready` = breakout close then retest-hold close (`awaiting_retest` replaces `awaiting_retest_hold`); Stoch history offset, most-recent divergence, stale divergence out of confluence; fade breakoutRisk `high`, no direction `unknown`; price-null / stale-flag-timeframe `DATA_UNAVAILABLE`; compact default `flagRecommendation` (full record under `model.recommendation`), slim failed candidates, 15m/1h candles 24 → 20, fixture byte-cap test; ledger id includes status + reasonCode; ready plans prefilled at the ready close in replay/ledger (gross R); `minNetRR` only in `flagPlan`. Schema 1.14.0 → 1.15.0, configVersion → 2026.09.23-4. Tests: scalp 109 → 113, flagplan 35 → 40, flagrec 12 → 17, ledger 10 → 12, replay 35 → 36, new `test:evidence` 8; others unchanged (sltp 50, mcp 52, wallet 28, config 14, risk 24, pattern 32, geometry 36, chart 18, bias 15, topdown 15, freshness 10). Not deployed. |
| 2026-09-23 | P1 Pyth mark (`docs/PLAN_PYTH_MARK_PRICE.md`): new `lib/pythMark.js` (one Hermes `/v2/updates/price/latest` request for all symbols, Bearer `PYTH_API_KEY`, 4 s timeout, never throws, key/URL never logged); `symbols.<SYM>.mark` beside `price` (`price` unchanged), `decisionTrace.bias` `|mark:<driftBps>`/`|mark:na`, compact mark `{price, driftBps, status}`; `mark.pyth` config block (feed ids, maxAgeSec 30, timeoutMs 4000). Schema 1.15.0 → 1.16.0, configVersion → 2026.09.23-5. Tests: scalp 113 → 117, new `test:mark` 12; others unchanged. Not deployed. |
| 2026-09-23 | Owner decisions 1a + 4a (`docs/OWNER_DECISIONS_2026-09-23.md`): `flagTradePlan.grossRR` published, plan gate on gross R:R ≥ `flagPlan.minRR` (renamed from `minNetRR`), reasonCode `rr_below_min` replaces `net_rr_below_3`, `netRR` information only; recommendation `rr_ok` support + non-blocking `net_rr_low` oppose, net never BAD; `room:blocked` reads only the candidate's mapped geometry timeframe. Schema 1.16.0 → 1.17.0, configVersion → 2026.09.23-6. Tests: flagplan 42 → 43, flagrec 17 → 18, pattern 32 → 33; others unchanged (sltp 50, scalp 117, mcp 52, wallet 28, config 14, risk 24, geometry 36, chart 18, replay 36, bias 15, topdown 15, freshness 10, ledger 12, evidence 8, mark 12). Live default 75,102 B, compact 39,948 B. Not deployed. |
| 2026-09-23 | Phase 2 recommendation completeness (`docs/MASTER_PLAN_NEXT_STEPS.md`): `lib/flagRecommendation.js` cites context on every record (top-down, EMA200 count/side, weekly EMA200 unknown, 4h lean, first level ahead on the candidate's own geometry timeframe, TP1 cap, channel-edge risk, candidate qual codes, divergence, freshness), names the nearest forming/triggering/proto flag as `candidate` on a no-plan WATCH, and writes concrete `changeConditions` (close/retest trigger, rejection remedy, or `a 1m/3m/5m flag must form`); BAD lists the disqualifying reason first. Class logic unchanged. `services/scalpContext.js` passes `candidates` + `geometryContext` into the recommendation. Schema 1.17.0 → 1.18.0, configVersion → 2026.09.23-7. Fixture payload default 76,864 → 78,003 B (cap 79,000), compact 41,459 → 42,598 B. Tests: new `test:flagrec:fixtures` 16; scalp 117, flagrec 18 unchanged in count (assertions updated for the new key/context). GPT instructions unchanged (7,976). Not deployed. |
| 2026-09-24 | T3 served calls (`docs/PLAN_SERVED_CALLS.md`): `GET /api/scalp-context` JSON 200 records the unfiltered payload's calls to Blob `served/` (1500 ms cap, swallowed, response unchanged, kill switch `TRACK_SERVED_CALLS=false`); `lib/blobJsonl.js` (journal blob helpers, moved) and `scripts/tracker/records.js` (tracker row builder, moved) shared; tracker `pullServed`, `source` dim, activity "Seen in chat", call log Via column. No payload/schema/config change; MCP untouched. New suite `test:served` 18; tracker 31 → 42; journal 17. |
| 2026-09-24 | T4 P0 flag paths (`docs/PLAN_FLAG_PATHS.md`, dev/tracker only, no engine or payload change): `scripts/tracker/flag-paths.js` labeller, `scripts/replay-paths.js`, resumable Kraken-trades 1m backfill + derived 5m/15m depth in `scripts/replay.js`, tracker `paths.js` step + `#flag-paths-section`. `lib/flagTradePlan.js` exports `observeRetestHold` (export only, for a parity test). 15-day base rates in `docs/FLAG_PATHS_BASE_RATES.md`. New suites `test:paths` 22, `test:replay-paths` 19; replay 36 → 41, tracker 43 → 52. |
| 2026-09-24 | T4 P1 pathOutlook (`docs/PLAN_FLAG_PATHS.md`, owner-approved): `scripts/build-path-table.js` (dev, `npm run paths:table`) builds a hierarchical backoff table (`tf\|structureSteps\|roomR\|compression`, dropping the last feature until n >= 100, down to `tf`, then `'all'`; separate `tightening`/`broken` tables) from the T4 P0 15-day labelled dataset (10,997 flags) into `config/engine.json`'s new `pathOutlook` key. `lib/pathOutlook.js` (pure; imports only `config/engine.js` and `scripts/tracker/flag-paths.js`'s `featuresAt`) picks the symbol's live flag candidate (`flagRecommendation.candidateId` if live, else `flagTradePlan.candidateId`, else nearest live forming/proto/triggering/confirmed) and resolves it to a bucket, publishing `symbols.<SYM>.pathOutlook` - info only, never touches `flagTradePlan`/`flagRecommendation`/`strategies`/`bestSignal`/any gate or the 3% scalp stop guard. Schema 1.18.0 → 1.19.0 (additive), configVersion 2026.09.23-7 → 2026.09.24-1. Fixture payload default 78,003 → 78,922 B (cap 79,000 unchanged), compact 42,598 → 43,517 B (cap 45,000 unchanged). New suite `test:outlook` 27; scalp/config/geometry/pattern schemaVersion assertions updated to 1.19.0, counts unchanged (scalp 117, config 14, geometry 36, pattern 33); mcp 52 unchanged, one read-only tool. |
| 2026-09-24 | T4 P4 breakoutEntry, SHADOW MODE (`docs/PLAN_FLAG_PATHS.md` "P4", owner wants it live/tracked, not promoted): `scripts/tracker/breakout-entry.js` (pure `shadowEntryFromBreakout`/`walkShadow` + vendored `netRiskReward`, copied flat into the tracker repo, imports only `./walk-outcome.js`) and `lib/breakoutEntry.js` (pure; imports only `config/engine.js` and the tracker copy) publish `symbols.<SYM>.breakoutEntry`: a breakout-close entry for the `pathOutlook` candidate when it is the FIRST close beyond `breakoutLevel` (`candidateSetups[].ageCandles === 0`) and `pathOutlook.chase` is elevated/high, gated on the same `flagPlan.minRR`/`scalp.maxStopDistancePct`, `status` always `"shadow"` - info only, never touches `flagTradePlan`/`flagRecommendation`/class/strategies/`bestSignal`/any gate/the 3% scalp stop guard/`minRR`. `lib/flagTradePlan.js` exports `netRiskReward` (export only, no logic change, for a parity test). Schema 1.19.0 → 1.20.0 (additive), configVersion unchanged. Fixture payload default 78,922 → 78,985 B (cap 79,000 unchanged), compact 43,517 → 43,580 B (cap 45,000 unchanged). New suite `test:breakout` 33; new dev script `scripts/replay-breakout-entry.js` (`npm run replay:breakout`) and `docs/BREAKOUT_ENTRY_SHADOW.md` (replay on the T4 P0 15-day/5,558-breakout dataset: shadow entry net expectancy negative and gross expectancy below the existing retest-hold entry on the same rows at every timeframe and chase bucket - **recommendation: do not promote to a real entry**, keep shadow-mode tracking). scalp/config/geometry/pattern schemaVersion assertions updated to 1.20.0, counts unchanged (scalp 117, config 14, geometry 36, pattern 33); flagplan 43, flagrec 18, flagrec:fixtures 16 unchanged (byte-identical with the new field present); mcp 52 unchanged, one read-only tool. |
| 2026-09-24 | T6 phase 0, fee-aware GOOD-call replay study (`docs/MASTER_PLAN_T6_FEE_AWARE_FLAGS.md`, research only, no deploy): `config/engine.js` gains `setConfigOverride` (deep-merge, in-process, live `ENGINE_CONFIG` binding, default off) and `config/engine.json` gains `flagPlan.minNetRR` (default `null` = off) - `lib/flagTradePlan.js`'s existing gross gate unchanged, the net check runs after it and only when set. `scripts/replay-rules.js` (`npm run replay:rules`) replayed 11 rule variants (net gate 1.0/1.5/2.0, `flag.timeframes` +15m/1h, combinations, an ATR-floored stop, a 15m/1h-structure stop/target, a FAILED_FLAG_REVERSAL scout research pass) through the production pipeline against 15 days of BTC/SOL/ETH history. Results and the recommended variant (V1c, net gate 2.0): `docs/GOOD_QUALITY_REPLAY.md`. Schema unchanged (net gate off by default, byte-identical payload). ConfigVersion 2026.09.24-1 → 2026.09.24-2. New suite `test:rules` 25; all other suites unchanged. |
| 2026-09-24 | T6 phase 1, fee-aware net gate ships on (`docs/MASTER_PLAN_T6_FEE_AWARE_FLAGS.md`, owner decision D1 from the phase 0 study, variant V1c): `flagPlan.minNetRR` null → 2.0; `flag.timeframes` unchanged (1m/3m/5m - V1c does not widen detection). `lib/flagTradePlan.js`: the net gate now actually rejects (`net_rr_below_min`, or `stop_inside_costs` when the round-trip cost alone is >= 0.5R of the stop's own risk - the BTC 0.066%-stop incident from section 1a is `stop_inside_costs`); new `costR` field published on every plan once entry/stop are known. `lib/flagRecommendation.js`: `net_rr_below_min`/`stop_inside_costs` follow the existing hard-rejection mapping (BAD, not DATA_UNAVAILABLE); `net_rr_low` renamed/split into `net_rr_ok` (support, the plan cleared the net floor) and `fees_heavy` (oppose, only reachable when the gate is off). GPT instructions: `flagTradePlan=trade authority` now names `net R:R≥2.0 after costs`; funded by tightening wording elsewhere, budget-neutral (7984 → 7978 units). Tracker: `scripts/tracker/build-page.js` `PHASE_NAME` → "Phase 5 forward record (net-gated rules)", `PHASE_START` → 2026-09-24, new `#testing-phase-restart-note`; 14-day/30-plan target and `phaseStartMs` aggregation unchanged, nothing deleted. Schema 1.20.0 → 1.21.0 (additive: `costR` field, two new `reasonCode` enum values), configVersion 2026.09.24-2 → 2026.09.24-3. Tests: `test:flagplan` 43 → 50 (net gate on by default, `costR`, `stop_inside_costs`, the real BTC-incident fixture); `test:flagrec`/`test:flagrec:fixtures`/`test:tracker` unchanged in count (assertions updated); all other suites unchanged. **Not deployed**: `npx vercel --prod --yes` hit Vercel Hobby's daily deployment cap (`api-deployments-free-per-day`); code committed and pushed, production still on the pre-phase-1 config until a retry succeeds. Tracker window restart intentionally held back until then. |
| 2026-09-24 | T6 completion plan Step A, review-pass fixes (`docs/PLAN_T6_COMPLETION_V2.md`, reviewer-consolidated; not deployed - stops for the gate). **A1 payload cap** (live had measured 79,863 B > the 79,000 cap): removed `breakoutEntry` from the payload entirely (shadow-mode research field, unpromoted per `docs/BREAKOUT_ENTRY_SHADOW.md`; `lib/breakoutEntry.js` stays, just unwired - shadow-mode data collection stops going forward, historical rows stay scorable); dropped `flagSlope`/`breakoutDistancePct`/`invalidationDistancePct`/`levelSource` from every candidate (`stripUnusedGeometryFields`, confirmed unread by the GPT instructions, tracker capture whitelist, or pathOutlook/flag-paths feature bucketing - still computed internally where used). A synthetic worst-case test (3 symbols x 6 simultaneous failed-in-TTL candidates + 1 ready GOOD plan each, replacing the old frozen-fixture cap test) measured 80,148 B after both trims; owner-approved minimal cap raise 79,000 → **80,200 B** (compact stays 45,000, already passed at 44,743 B) rather than a deeper field cut. **A2**: `net_rr_below_min`/`stop_inside_costs` already had the hard-rejection mapping and openapi enum from phase 1; added the missing direct `flagRecommendation` tests (primary reason text, remedy text citing the net floor / cost fraction). **A3 (correctness fix)**: a retest candle whose wick reached through the plan's own stop before closing back on the hold side was being credited as a valid hold - in a live position that wick would have stopped the trade out already. Fixed in `lib/flagTradePlan.js`'s `observeRetestHold` (new `stopBreached` check) and its byte-for-byte vendored copy in `scripts/tracker/flag-paths.js`, plus the same check added to `labelPath`'s own `retestReachedHeld` (a stop-wicked 1m touch no longer counts as a retest, so mislabelled `retest_go` rows become `runner`). **A4 (correctness fix)**: the blob first-write race - two concurrent requests creating the same day's blob both read no existing file, both `put()`ed with no `ifMatch` (the guard only applied to updates of an already-existing blob), and whichever won the network race silently overwrote the other's row with no error and no retry. `lib/blobJsonl.js`'s `writeBlob` now asks for `allowOverwrite: false` on a create (no `etag`), so a losing concurrent create throws `BlobAccessError` instead of overwriting; `updateBlob` retries that the same as an ETag conflict (`isOverwriteConflict`, `WRITE_ATTEMPTS` unchanged at 3). **A5 (latency fix)**: `api/scalp-context.js` awaited served-call recording (`recordServedCalls`, 1500 ms cap) *before* sending the response, adding real latency to every request even in the non-hanging case. The response is now sent first; recording happens after, still capped and swallowed, never able to affect an already-sent response. **A7**: `setConfigOverride` now throws if an override touches `configVersion` or `thresholds` - both are snapshotted once at module load elsewhere (`config/engine.js`'s own `CONFIG_VERSION`, `services/strategy.js`'s `THRESHOLDS`) and would silently not take effect, a correctness trap for a future replay variant; `test-replay-rules.js`'s end-to-end section now fails (not silently skips) when its gitignored fixture is missing. **Open, not implemented (ask the owner, A7)**: whether `lib/flagTradePlan.js`'s TP1 cap (`nearestRoomAhead`) should read only the candidate's own mapped geometry timeframe, matching owner decision 4a's scope for `room:blocked` (`docs/OWNER_DECISIONS_2026-09-23.md` item 4) - left as-is (reads every geometry timeframe) pending an answer. Schema 1.21.0 → **1.22.0** (`breakoutEntry`/`flagSlope`/`breakoutDistancePct`/`invalidationDistancePct`/`levelSource` removed - not additive, but all five were unread by any consumer; `BreakoutEntry`/`LevelSource` schema components deleted). Tests: see the counts addendum above; 848 tests across 27 suites, all green; `check:gpt` OK, `git diff --check` clean. |
| 2026-09-24 | Owner decision 4b (`docs/OWNER_DECISIONS_2026-09-24.md`): `room_at_entry` (hard rejection) now reads only the candidate's own mapped geometry timeframe, matching decision 4a's scope; the TP1 cap (`nearestEdge`) keeps reading every geometry timeframe, unchanged ("major S/R overrides"). No config/schema change. `test:flagplan` 51 → 53. |
| 2026-09-24 | T6 completion plan Step B2, D-cost/D-variant answered (`docs/OWNER_DECISIONS_2026-09-24.md`; not deployed - stops for the gate). **D-cost**: net-R plans around a direction-dependent round-trip cost (positions funded USDC/USDT) - long 0.34%, short 0.14%, fallback 0.20%. **D-variant**: stay on V1c live; run V-B (gross minRR 2.5) as an engine-computed shadow instead of shipping it (revisit 2026-10-07, n >= 20 each side). `lib/flagTradePlan.js`'s `buildFlagTradePlan` gains an optional `shadowVariants` list - re-attempts the same candidate pool per variant with `flagPlan.minRR` overridden (same ATR/candles/retest-hold as the live plan, no approximation), publishes `shadow.<id>` only when that variant's outcome differs from the live plan. `services/scalpContext.js` wires `shadowVariants: [{id:'vB', minRR:2.5}]`; `flagTradePlan.shadow` is stripped from every response (include-less default too) unless `include=model`, always captured in the served-call record (pre-filter payload) for the tracker. New `scripts/tracker/vb-shadow.js` (shadow-mode scoring, `walkShadow`, same dir-cost model) + new "V-B shadow" tile on the call tracker page (`build-page.js`) + a new pipeline step in `track.yml` (repo-template, not yet synced/pushed). `scripts/replay-rules.js` gains `netR_sensDir`/`netExpectancyR_sensDirPct` (the same dir-cost applied to the offline replay research, `docs/GOOD_QUALITY_REPLAY.md`'s Step B table). Schema 1.22.0 → **1.23.0** (additive: `FlagTradePlan.shadow`), configVersion unchanged. Tests: `test:flagplan` 53 → 59, `test:scalp` 117 → 120, `test:tracker` 85 → 93; `test:config`/`test:geometry`/`test:pattern` schemaVersion assertions updated to 1.23.0, counts unchanged. Full gate green. |
| 2026-09-24 | T6 completion plan Step C (owner-approved after Step A + B2 deployed live at schema 1.23.0; not deployed - stops for the gate). **C1 (dir-cost, D-cost decision applied live)**: `config/engine.json` gains `risk.costBpsByDirection: {long:34, short:14}`; `lib/flagTradePlan.js`'s `costRFraction`/`netRiskReward` take an optional `direction` param (own the dir-cost split when given, unchanged flat `feeBps`/`slippageBps` fallback when omitted - `scripts/tracker/breakout-entry.js`'s vendored `netRiskReward` never passes it, so that parity test is unaffected); `flagTradePlan.netRR`/`costR` are now direction-dependent on every plan/shadow variant. `scripts/tracker/costs.js` mirrors it (`COST_BPS_BY_DIRECTION`, `costR`/`netR` take `direction`) so `aggregate.js`'s real GOOD-call net expectancy and `vb-shadow.js` share one cost model - `vb-shadow.js` now imports `costs.js`'s `netR` instead of its own duplicate constants. `buildConfigSnapshot` publishes `config.risk.costBpsByDirection`. A real behavior change, not just reporting: a long's netRR is now harsher (34 vs the old flat 20 bps), a short's easier (14) - several existing test fixtures with symmetric-by-construction numbers needed new risk distances to keep clearing/failing the net gate as intended (documented inline, T6 completion plan C1). `scripts/tracker/breakout-entry.js`'s own vendored `netRiskReward` (T4 P4, unpromoted per `docs/BREAKOUT_ENTRY_SHADOW.md`) deliberately NOT updated - frozen/unpublished feature, out of scope. **C2 (SETUP tier)**: `flagTradePlan.setup` (computed in `buildFlagTradePlan`, reusing its own `attempts` pass - the best still-`conditional` candidate in the pool, `null` when none) copied through to `flagRecommendation.setup` and stripped from `flagTradePlan` itself before publish (byte budget - never serialized twice). Default payload (not `include`-gated). Tracker: `score.js`'s `kind:'setup'`/`counterfactual_setup` (what-if, walked only from an actual entry touch, never prefilled); `aggregate.js` tiles `goodPerHour7d`/`setupsPerDay7d`; `charts.js`'s `setupEquityRows` feeds the equity filter table (its own "SETUPs, what-if" row, not a drawn line - scoped down from a full third SVG series, a disclosed trade-off). **C3 (tracker)**: `aggregate.js`'s `configBoundary` - the most recent `configVersion` transition across tradable rows (each already carries its own capture's `configVersion`), gross+net stats split before/after, rendered as a page note when present (null/no note on a single-version window). "Net and gross R side by side": `charts.js`'s `chartKit` gains a vendored dir-cost `netRFor` (equity/journal/setup rows now carry `entry`/`stop`/`dir`), `equityStats` computes `netExpectancy` alongside `expectancy`, threaded into `readoutHtml`/`youReadoutHtml` (new NET span) and `filterTableHtml` (new "Net exp. (dir-cost)" column). **C4**: `FLAG_TF_RANK` (`lib/flagTradePlan.js`)/`FLAG_TF_ORDER` (`lib/flagRecommendation.js`) gain `15m`/`1h` entries (inert today - `flag.timeframes` stays 1m/3m/5m per the D-variant decision, V-C not chosen) so a future widening needs no ranking-logic change; documented in this doc rather than shipped as a config change. **C5**: GPT instructions - `flagRecommendation.setup≠null` rule (CANDIDATES), `SETUP LINE` template (FORMAT, after NO TRADE LINE), `signals` appends it (COMMANDS), `flagTradePlan=trade authority`'s net-R:R clause now says `dir-priced`; mark rule (RISK) untouched. Payload-field table gets `flagRecommendation.setup` and `config.risk.costBpsByDirection` rows; test sheet prompt 18 added. Funded by tightening wording elsewhere (STYLE, CANDIDATES), no rule cut: 7744 → 7988 units (2 headroom). Schema 1.23.0 → **1.24.0** (additive: `FlagTradePlan.netRR`/`costR` direction-dependent computation - same fields, no shape change; `FlagRecommendation.setup`, new `SetupTier` component; `ConfigSnapshot.risk.costBpsByDirection`), configVersion 2026.09.24-3 → **2026.09.24-4**. Tests: `test:flagplan` 59 → 67, `test:tracker` 93 → 110, `test:flagrec` 20 → 23, `test:scalp` 120 → 121; `test:config`/`test:geometry`/`test:pattern` schemaVersion assertions updated to 1.24.0, counts unchanged (19/36/33); deploy gate unchanged otherwise (sltp 50, mcp 52, wallet 28). 26 suites, all green; `check:gpt` OK (7988/7990); `git diff --check` clean. |
| 2026-09-24 | Delivery pass (no rule/threshold/config change; not deployed): `flagRecommendation.action` (GET IN NOW / BE READY / WAIT / STAND DOWN with etaMin/at to the next close) and `flagRecommendation.room` (pts and R to the first level ahead vs the stop) in the compact and full records; GPT instructions - `[CALL]` prefix on asset lines, `Room:` on NO TRADE/SETUP lines, one-line DATA, compact-only Action call, SETUP LINE only when setup≠null (7939 units / 7982 bytes); Telegram - `/signals` mirrors that format, GOOD/SETUP/WATCH/TRIGGERING/BREAKOUT alerts carry the call and room line. Schema 1.24.0 → 1.25.0, configVersion unchanged. |
| 2026-09-25 | Telegram sent-alert + transition logs (logging only; no rule, threshold, schema or config change; not deployed): new `lib/telegramLog.js`; the cron appends one line per sent alert to Blob `telegram/alerts/YYYY-MM-DD.jsonl` and one per candidate state / plan-status change (`diffCandidates`, `state.cands`) to `telegram/transitions/YYYY-MM-DD.jsonl`, each with a day manifest, after the sends (2 s cap, never blocks a send, `TRACK_TELEGRAM_LOG=false` kill switch). Tracker: `pullTelegramLogs` → `data/telegram-alerts/` + `data/transitions/`, `scoreAlerts` → `data/alert-outcomes.jsonl` (latency, outcome, later GOOD, ready-after), `aggregate.js` `alerts` block, page `#zone-alerts`. `test:telegram` 85 → 90, `test:tracker` 111 → 114. |
